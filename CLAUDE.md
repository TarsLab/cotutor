# cotutor

家里的 AI 老师团,和家长一起教(应用仓,全新项目,2026-09-08 起)。设计文档在 `docs/`:《产品规划.md》(定位、两个端、回复形式阶梯、里程碑 R0–R8、拍板记录)、《agent层设计.md》(目录、三种记忆、子代理、规划老师)、《契约草案.md》(各文件形状,已逐条拍板;契约的真相是 `src/schema/`,它只剩「为什么」)、《工作流程.md》(2026-09-13 拍板:一拍一就绪、快模型按拍带前文、控制台分道事件;落地五步)、《快模型方案.md》(后期的提示词成文件、样本即测试、评测命令、时间预算)、《obsidian仓库设计.md》(2026-09-14 拍板:vault 是长期记忆、六种文件、日记是观察的真相、话题打分 ≥ 4 才沉淀、代码不认题型只认教材单元锚点、机器写 vault 的封闭清单)。拍板都在那里;本文件只写这个仓怎么长的。草稿与随手记放 `docs/wip/`(gitignore,不入库)。底层工具 drawtell / voxtell 按 npm 版本依赖,不在本仓;要改 drawtell 时开发期临时 `link:../drawtell`(2026-09-15 起就是这样:等 drawtell 0.7.0 发了换回 `^0.7.0`,并把新版本加进 pnpm-workspace.yaml 的 minimumReleaseAgeExclude),发 cotutor 前必须换成版本号。**drawtell-skills 仓 2026-09-15 退役**:四个领域 skill(cli / scene / teaching / verify)随 drawtell 包发(它的 `skills/`),其余六个(入口、vault、homework-*、math-explainer、reading-book)编码的是被《obsidian仓库设计.md》与 scene-maker 老师文件取代的老工作流,不再维护;drawtell 的 `tidy` 同日删。

## 结构(单包,subpath export,同 drawtell)

- `agents/*.md` — **老师文件**(frontmatter 取两 CLI 公共子集:name / description / maxTurns / permissionMode / memory,正文即系统提示)。出厂六位:math / chinese / reading / homework / planner(hidden)/ scene-maker(hidden,画图老师:别的老师「## 转交」一道题过来,它用 drawtell 做课包到 bundles/<id>/,工作流写在正文,点名四个领域 skill;runtime 用出厂的 `claude-scene`,预算 8 美元)。是出厂件:init **拷贝**进 workspace 的 `.claude/agents/`(2026-09-09 拍板,原为链),出厂 hash 记 `.cotutor/shipped.json`,`cotutor upgrade` 据此换新或报 diff(`src/cli/tutors.ts`);人设、政策、开关不在这里,在 workspace 的 cotutor.json
- `themes/default/` — **出厂主题**(孩子端板书的样子):`theme.json` 是槽的清单(tints 底色槽 / looks 字形槽 / pens 五支笔,每槽一个名字 + 一句「给什么用」,槽任意多,`default` 必须在 tints 里),`kid.css` 是值与样式(板、卡、笔、舞台壳;页面骨架仍在 kid-page.ts)。是出厂件:init **拷贝**进 workspace 的 `themes/default/`,目录 hash 记 `.cotutor/shipped.json` 的 themes,`cotutor upgrade` 同一套(`src/cli/themes.ts`);`cotutor add-theme <name>` 拷一份来改,cotutor.json `kid.theme` 指过去;服务 `/kid/theme.css` `/kid/theme.json` 按 mtime 现读(`src/server/theme.ts`),读不到 / 坏了退包里的 default,doctor `theme.manifest` 点名;舞台包的 index 也 link 它。契约 `src/schema/theme.ts`。2026-09-12 样子按原型落地:板 `#faf9f4`,卡 `.c[data-tint=<槽>]` 底色 + 同系 1.5px 描边、`data-look=<槽>` 字形,选中态 `.c.now` 主色描边 + 外晕;面类笔(marker / tint)是 span 背景,线条类(underline / box / circle)是贴在卡上的 SVG 路径(`.pen`),念到时 `stroke-dashoffset` 描出来
- `cards/<kind>/` — **卡的协议文件**(2026-09-12,包根,进 npm files):`card.md` 八个固定栏目(是什么 / 何时用与别用 / 写法 / 例子 / 反例 / 孩子看到什么 / 你会收回什么 / 状态的形状;例子前一行 `<!-- expect {…} -->`,**例子即测试** `tests/cards-doc.test.ts`),`card.css` 这种卡的结构样式(只用主题变量;服务把八份拼在主题 css 前面出 `/kid/theme.css`,主题写同名选择器能盖)。`src/cards/docs.ts` 读它们:`boardSyntaxDoc()`(从各 md 的 是什么 / 写法 / 例子 拼语法表)、`boardSkillDoc()`(frontmatter name / description + 语法表 = SKILL.md)、`boardSkillFiles()`(技能目录的全部文件)、`cardsIndexDoc()`(索引)、`cardDocs()`、`cardsCss()`。**给老师看的那份是技能 `cotutor-board`**(2026-09-14 拍板,原来是 `.cotutor/板书语法.md` + `.cotutor/cards/`;真跑 32 轮老师只读过语法表 1 次、单张协议 0 次,改成技能是为了靠 description 被发现、不再写 `../../.cotutor` 相对路径):`pnpm run gen:skills`(`scripts/gen-skills.ts`)生成到**包根 `skills/cotutor-board/`**并入库(SKILL.md + `references/<kind>.md` + `references/README.md`),`tests/skills.test.ts` 断言入库的和生成的一致(改了 card.md 忘了跑就红;`UPDATE_SNAPSHOTS=1` 也重写);出厂到 workspace 走 skills.ts 的出厂表,`machine: true`:init / upgrade 每次按包里覆盖、家长改了也刷,doctor `skill.cotutor-board` 必需、`board.legacy` 旧位置还在就提醒开发者那份在 `docs/卡片协议.md`(加一种卡的七步)
- `src/cards/` — **卡的注册表**:每种卡一个文件(props zod、围栏正文 `parse`、`strip` 剥答案、有交互的再加 `state` zod 与 `describe(props, state)` 给老师的一句、要预生成配音的再加 `assets(props)`;写法文档不在 ts 里,在 `cards/<kind>/card.md`),`index.ts` 登记 + `parseCard`(不认识的标签当 code 卡,解析失败退 text 卡 + warning,永不抛)+ `stripSecrets`(答案剥掉、孩子的 state 照给)+ `parseCardState` / `describeCard` / `cardLabel` / `cardAssets`;现有 text(修饰 cover / note / quote / formula / step)、read(assets:一段一个 `<k>.mp3`)、choice(state `{picked[]}`,多个 `[x]` → `multi`)、fill(state `{answers[]}`)、image(相对 workspace 的路径或 http 地址 + 图注)、scene(正文课包 id;title / problem / steps / ready / thumb 是下发时 `server/scene-props.ts` 从 bundles/<id>/ 现读的快照;state `{step, done}`)、canvas(底图课包 id / 行内骨架 / **一张图的路径**(R5,2026-09-14:孩子拍的 `captures/…` 做底,舞台装成 excalidraw 的 image 元素)/ 空白 + 题目;state `{ink[], image?}`,png 由 PUT 时服务端落 `.cards/<n>.png`)、code。页面那一半在 kid-page.ts(紧凑态 + 舞台态渲染)与 kid-board.ts(标注落点、`hasState` / `cardTitle` / `togglePick` / `pickedLabels` / `filledAnswers` / `stateSummary` / `segmentAudio`),按 kind 名对上;加一种卡 = 加一个文件 + 一行登记 + `cards/<kind>/` 两个文件 + 页面一个分支(`docs/卡片协议.md`)
- `src/schema/` — **契约**(zod,类型即文档;`explainIssues` 把 issue 变修复指南):config(cotutor.json,含 runtimes / tts与 server.https,字段带 `.describe()`;paths 角色缺省中文名 日记 / 计划 / 孩子.md / 课程表.md / 教材 / 参考,captures 相对 workspace 根;`vault.keepScore` 话题打几星起记账才沉淀摘要,缺省 4;policy.contextPack 加 profileLines)、context-pack(加 `profile:` 档案「现在」几行;`photos:` 这条消息的作业照片一行一张,R5)、conversation(消息带 section / parentText / `photos[]`(相对 workspace 根,R5)/ timing:startedAt + firstCardMs / doneMs / dubbedMs,从进程起算的毫秒;`thread` = 话题 id = 话题首条的 job,索引 `sessions[thread]` 每个话题自己的会话、顶层 `session` 只是当前话题的;旧索引缺这两样按 `threads()` 现算;2026-09-14 加索引级 `ratings[thread]` 1–5 星与 `booked[thread]` 记账那轮的 job,消息级 `bookkeep {thread}` 这轮是记账任务、`bookkeeping` 物化的「## 记账」段)、board(板书节:卡 {kind, props} + 讲稿句;类型真相在 lib/kid-board.ts,zod 对齐)、ledger(只剩产物账本 artifacts.jsonl;observations.jsonl 2026-09-14 退役,观察的真相在 vault 的日记)、sections(待裁量 / 转交 / **记账**(`BookkeepingSchema`:entries[{thread, name, textbook?, summary?, steps?, observations[]}],字段带 `.describe()`,cotutor-vault 的 references/记账.md 从它生成);`lineMap` 把 body 行号映回原文)、plan、timetable、json-schema(`cotutorJsonSchema()` 从 zod 现生成,init / upgrade 写到 workspace 的 `.cotutor/cotutor.schema.json`,cotutor.json 首行 `$schema` 指过去)
- `src/lib/` — **纯函数**(全部离屏可测):agent-file、sections(三种固定段的宽容解析,「## 记账」漏写 `- thread:` 也认)、**diary**(2026-09-14,《obsidian仓库设计.md》:`diaryTopic` / `renderDiaryBlock` 把记账段 + 索引里的话题渲染成日记一段 `## 学科 · 话题名` + 孩子问 callout + 摘要 ★ `[[册#节]]` 课包 + `骨架:` + `- 观察:` 行,打分 < keepScore 丢摘要与骨架;`appendDiary` 只追加;`extractObservations` 从最近几天日记抽「- 观察:」行(按 H2 的学科过滤、围栏跳过)给上下文包 recent;`extractProfile` 取档案「> [!abstract] 现在」callout;`textbookHeadings` 教材目录的「册#节」;`bookkeepingPrompt` 记账任务的消息正文;`entryFor`)、transcript(stream-json → 家长条目 + 最终文本,子代理事件标 sub;**`toolCalls(log)`**(2026-09-15,记录层)把 tool_use 与 tool_result 按 id 配对成 `{name, arg, ok, chars, sub}`,arg 取路径 / 命令 / pattern in path,runner 收尾物化进消息 `tools`)、board(**板书解析器** `parseBoard`:正文里围栏 = 卡、普通行 = 讲稿一句、第一个 H2 起是给家长的尾巴;`[词]` 标注整节找、`[[名 参数]]` 是对上一张卡的 cue、句子锚到上一张卡;`partial` 模式压住没闭合的围栏与没换行的末行,给流式用;还返回 `spans`(每张卡 / 每句是原文哪几行)与带 `line` 的 warning,`annotateSource` 据此把老师原文逐行标上「解析器把它当什么了」——它真跑 parseSections + parseBoard 再把行号映回原文,不另写一套,所以不会和真解析漂开)、kid-view(孩子视图:剥段 → parseBoard → kidText = 讲稿各句拼起来、每句按 replyMaxChars 截;`kidConversation` 把索引过滤成孩子端条目并 `stripSecrets`)、events 还有 `beatTimings(events, beats)`(每拍的关 / 配音齐 / 后期 / 就绪,从事件推,进 `timing.beats`)与 `timelineSpans(events)`(甘特的段:老师起→退、配音每句排队→完成、后期每拍起→回,其余瞬时;没收尾的画到末尾标 warn);kid-board(板书的 JSON 契约 `BoardSection` + 孩子端全部交互逻辑:标注锚点、播放状态机、字幕行、输入条;**不能有运行时 import**,kid-page 把它剥掉类型内联进页面,两处一份源码;2026-09-12 加:卡的 `look {tint, look, emoji}`、标注的 `pen`、节的 `layout {for, rows}` 三个可选字段(板书后期填,没有走机械规则 `tintFor` / `lookFor` / `penFor`);**拍**(2026-09-13,《工作流程.md》§二):`beatsOf(section)` 按锚点分组(卡前的句是没有卡的一拍;不进契约,现算)、`readyBeats(section, {voiced, done})` 前几拍就绪(关了 + 每句配音落盘;没配音色不等)、`playableLines`;流式的节带 `ready`,`advance` 播到就绪的头停成 `thinking`(新 PlayStatus:老师还在写,等下一拍),`subtitleFor` 正在播照常出字幕、没在播才「让我想想…」;`rowsFor(section, device)` 按端折行(同端照排、手机上两张都短才并排、标题行与有状态的卡独占)、`deviceFor(w, h)`、`nowCard` 选中态、`penBox` / `penPath` 线条笔的 SVG 路径(种子取词的 hash);text 卡 `heading: true` 是小节标题行,不算卡;2026-09-13 加:标注的 `said`(讲稿里念到它的那个词,后期填)与 `markTiming(line, mark, totalMs)`——没有字级时间戳(voxtell align 还是规划),按字数比例估这处标注在这句里什么时候画、描多久,词在讲稿里找不到 → null 句首就画)、stream(`createPartialReader`:从 stream-json 增量里拼当前顶层回复,claude 的 delta 与 qwen 的整条 assistant 都认,子代理跳过)、context-pack(`focus.card`、`cards:` 段)、plan、ledger、events(**一轮的事件**,2026-09-13,《工作流程.md》§五:`RunEvent` 七道 main / tts / post / ready / index / handoff / ledger,`t` 从老师进程起算;`describeEvent` / `formatEvent` 一行 = 相对秒 · 道 · 一句话,`laneFilter('post,tts')`,`parseEvents` 读 jsonl 坏行跳过;发事件的在 runner)、conversation(文件命名含 `.cards/<n>.json`、`.events.jsonl`;`threads` / `currentThread` / `sessionFor` / `lastJobOf` 话题的纯规则:第一条与 from: system 各开一个、其余跟前一条;`changedCards(index, states, thread)` 挑这个话题里上一轮之后改过的卡;`applyRun` 按话题记会话,话题首条一律新会话)、timetable(抄 growth-apps,孩子列可选)、icon(主屏幕图标与 manifest:PNG 按尺寸**现画**——没字体可用,所以是纯几何标记,3 倍超采样 + 自己编 PNG(只 node:zlib),同尺寸缓存;`webManifest(title)` 是 standalone 清单)
- `src/lib/post-fixtures.ts` — **后期的样本即测试**(2026-09-13,《快模型方案.md》§二 B / C):`tests/fixtures/post/<名>/` = `section.md`(老师原文,同 fixtures/board)+ `expect.json`(部分匹配:must 该标的 / never 不该提的 / rows / look 允许的槽 / maxDropped)+ `prompts.md`(各拍提示词的渲染快照,测试生成入库,改骨架 diff 里一眼看到;`UPDATE_SNAPSHOTS=1` 重写)+`prompts-json.md`(同一样本按老的 JSON 骨架渲染的快照)+ `runs/`(评测留档,gitignore);`loadPostFixture` / `renderFixturePrompts` / `expectProblems` / `scoreFixture`。离线测试 `tests/post-fixtures.test.ts`(快照、期望合法、must 校验器收得下、never 里老师已标的校验器会丢);真模型评测 `scripts/post-eval.ts --workspace <dir> [--only 名] [--repeat n] [--dialect json|html] [--template 文件] [--serial] [--model 名]`(走 runPost,缺省各拍并行,`--serial` 顺着起、前文带已定的样子和流式一样;`--dialect` 用代码里的两份骨架比方言、`--model` 换掉运行时命令里 `--model` 后面那个(sonnet 2026-09-14 跑过:结论同 haiku,贵 3 倍),都不动 workspace;,打印 must 命中 / never 失手 / 行 / 底色 / 丢 / 每拍 p50 p95 / 费用;2026-09-13 haiku 首跑:8 拍 7 成、never 失手 0/9、p50 4.4s、p95 10s(四拍并行时一拍超时)、$0.116)。调骨架的日常:改 post.md → pnpm test 看快照 diff → post-eval 看分
- `src/lib/post-html.ts` — **后期的 HTML 方言**(2026-09-14,《快模型方案.md》§二 E:HTML 进、补丁出):板书按孩子看到的结构给模型看,**全是标准 HTML、自定义的都是 data-\***(2026-09-14 晚拍板):`<div class="row">` 一行、`<div class="c c-<kind>" id="cN" data-tint data-look>` 一张卡、这一拍的卡 class 带 now、讲稿 `<p class="line" data-n>`;**已画的标注不画在卡上**:卡与讲稿句各挂 `data-marked`,另给文字列表 `{marked}`(`markedBlock`,按卡列)——三种写法评测都是再标已画的 7–10/18,那是它看见全文后总挑老师挑过的那个词,校验器丢掉、孩子看不见,别当退步;类名与孩子端页面同一套,CSS 一行不给),模型回的补丁就是 now 那张卡的壳 `<div class="c" id="cN" data-row data-tint data-look data-emoji>`(里面 `<mark data-pen data-said data-card data-line>词</mark>` 与空的 `<p class="line" data-n data-for></p>`,正文不抄;老的 `<c row><mark pen><line n for/></c>` 解析器照认);`cardHtml` / `boardHtml` / `lineHtml` 渲染,`parseBeatPatch` 宽容解析(围栏 / 引号 / 自闭合 / 实体;词落在哪句靠 said 或词本身在这拍讲稿里找,不用模型数下标)成同一个 `BeatPostOutput`,校验器不分方言。骨架用了 `{board}` 就是 html 方言(`dialectOf`,必需占位符 board / rules / patch,`{marked}` 可选;`{rules}` 按方言渲染),用 `{cards}` 就是 json;`POST_TEMPLATE_HTML` 是它的骨架(**板书放最前、任务在后**:夹在中间 haiku 把整份当系统说明、回「我还没看到板书」),`.post.json` 记 `dialect`;假 CLI 见 class 带 now 的卡就回补丁。**2026-09-14 晚拍板:出厂缺省换成 html**(`themes/default/post.md` 与 `POST_TEMPLATE_FALLBACK` 都是 `POST_TEMPLATE_HTML`;JSON 骨架留作 `POST_TEMPLATE_JSON`,老 workspace 里拷过的 post.md 还是 json 方言,照跑,`cotutor upgrade` 报 diff;快照 `prompts.md` 是出厂的、`prompts-json.md` 是 json 的)——真跑 16 拍 ×6 轮:HTML 底色 8/8 对(json 5/8)、下标错误归零、最新一版 16/16 p95 6.4s;never 失手 10/18 是它总挑老师已挑过的词(json 0/18 是因为它挑的是讲稿里的词,一样被丢),两种方言在样本上都没贡献过新标注;数据在《快模型方案.md》§二 F
- `src/lib/diff.ts` — 行级 diff(LCS)`diffLines` 与只留变处的 `compactDiff`,给回放对照用
- `src/server/replay.ts` — **回放**(2026-09-15,评估改动效果的主路,只有 CLI):`evalWorkspace` 把 Workspace 的对话目录换成 `evals/`、老师全开、不配音、后期缺省关,`startReplay` 用它 new 一个 Runner 把原轮的问题(from / text / photos / action / device)按 `newThread` 再发一遍(上下文包按现在重拼;原轮的 cards: 段带不上,notes 说明),消息带 `replayOf`;`compareReplay` → `Compare`:两侧 `RunSide`(讲稿、卡、tools、费用、timing、家长尾巴、run.json 的 prompt 与老师文件 hash)+ 上下文包 / 讲稿 / 卡 / 读了什么四份 diff + notes(老师文件变没变);`formatCompare` 终端打印;`listReplays`。evals/ 在骨架 .gitignore 里
- `src/lib/run-plan.ts` — 一次运行的命令规划:同运行时有会话 → resume,否则新开;`{agentBody}` 只给模板里用到它的运行时
- `src/stage/` — **舞台包**(React):重卡(scene / canvas)的播放器与编辑器(canvas.tsx 的底图第三种 = 照片:页面给 `imageUrl`,`loadPhoto` 取回来做 excalidraw 的 image 元素 + files 一份,长边 1200、锁定、灰一档、layer base,导出恢复原色;`window.__excalidraw` 是给探针的钩子),`scripts/build-stage.mjs` 用 esbuild 打成 `dist/stage/`(**splitting: true**,2026-09-11:整包 8MB 但 mermaid / cytoscape / katex 是 excalidraw 自己 `import()` 懒加载的,不拆会被全部内联进一个文件;拆开后 stage.js 21K + 静态 chunk 共 1.4MB,真开一次场景舞台下 3.4MB、画板舞台 1.5MB,拆包前两者都是 8.6MB。画板编辑器再由 main.tsx 的 `lazy(() => import('./canvas.tsx'))` 单独成块。alias 把 react / react-dom / excalidraw 钉到本包 node_modules,免得 link 进来的 drawtell 带第二份 React;excalidraw 的 css 按 `dist/prod/index.css` 引,CSS 不拆),`pnpm run build:stage`,prepublishOnly 也跑;孩子端页面在 iframe 里装 `/stage/?card=<id>`,postMessage 协议在 `protocol.ts`(card / control ↔ ready / phase / state / submit / close / error);`?bundle=<课包 URL>&autoplay=1` 不用页面也能开。scene.tsx = drawtell 的 ChalkPlayer(controls 关,stepIndex 0 起、对外报 1 起),canvas.tsx = 画板工作台(2026-09-12):excalidraw 只当画布,它的工具栏 / 菜单 / 缩放 / 右键菜单全用 stage.css 藏掉,工具是自己的七个钮(笔 / 橡皮 / 黑红蓝 / 撤销 = 抹孩子最后一笔 / 清空要点两下),竖屏一行、横屏一列(纯 CSS 媒体查询);题目条可收起;底层 locked + layer base + opacity 45 灰一档,孩子的笔 layer ink;「给老师看」exportToBlob 出 png 时底图恢复原色;页面的舞台顶栏对画板只写「画一画」,动作条左边「画了 N 笔」;交过之后紧凑态是缩略图(真服务 png 路径,mock data URL);手动验收 `node scripts/probe-canvas.mjs`(CDP:iframe 里 PointerEvent 画三笔 → 撤销 → 给老师看 → 缩略图))。tsconfig 有 jsx + DOM lib,tsconfig.build 排除它
- `src/cli/` — workspace(解析链 + 校验;`resolvePaths` vault 侧角色相对 vault、`WORKSPACE_ROLES`(captures)相对根)、migrate(老 workspace 的配置迁移:`configGaps` 现算差异、`upgradeConfig` 只补缺)、skeleton(骨架清单 + 模板,init 与 doctor 共用;**家规 CLAUDE.md / QWEN.md 2026-09-15 起不出厂**:老师要知道的都在老师文件与技能里,家长要共同规矩自己在根建,doctor `rules` 只提一句;删的另一个理由是板书后期的 `claude -p` cwd 是根,家规被塞进每一拍;作业老师出厂 `enabled: false`(R5);`.gitignore` 模板含 `captures/`;`profileTemplate` 档案与 `REFERENCE_README`,init 缺了才写到 paths.profile / paths.reference;板书语法表不在这里,在 skills.ts 的 `writeBoardSkill`)、tutors(老师文件拷贝 / 状态 / upgrade / add 模板 / 页面读写删)、skills(**出厂表 `SHIPPED_SKILLS` 每项带来源与 machine 标记**,2026-09-15 照 hyperframes 的 `skills/` 目录:来源 `cotutor` = 包根 `skills/<name>/`(进 npm files),来源 `drawtell` = drawtell 包根 `skills/` 的四个领域 skill(`drawtellSkillsDir` 用 createRequire 解析 `drawtell/package.json`;没装 → unavailable);都拷进 `.claude/skills/`、hash 记 shipped.json、`.qwen/skills/` 相对链;`machine: true`(cotutor-board / cotutor-vault)每次 init / upgrade 按包里覆盖、状态只有 latest / upgradable、`legacy` 旧位置顺手清,其余拷进来就是家长的,upgrade 改过的报 diff 保留;`.cotutor/drawtell` 壳脚本指向本包 node_modules 的 drawtell,机器文件)、themes(出厂主题拷贝 / 状态 / upgrade / add-theme;`readManifest` 现在保留 skills 与 themes 两段,以前 tutors 一写就把 skills 抹了,靠顺序碰巧没出事)、usage(USAGE 文本,analyze-doc 也读它)、init、doctor(`captures` 照片目录在根以内、可写、几天(还没拍过也绿);`vault.profile` 档案有没有「现在」callout、`vault.diary` 与 `.obsidian/daily-notes.json` 的 folder / format 对一次、还留着 observations.jsonl 只提醒;`--live` 真起一次老师与配音,`explainLlmFailure` 把 API 层错误变修复指南;`env.nested` 认 CLAUDECODE)、upgrade、add、serve(机器级 `~/.config/cotutor/certs/` 有证书就 HTTPS,`server.https` 是单 workspace 例外)、cert(mkcert 签到 `~/.config/cotutor/certs/`,不需要 workspace)、send(终端发一条,与页面同一条路)、rate / bookkeep(main 里:`cotutor rate <老师> <话题> <1-5> [--date]`、`cotutor bookkeep <老师> [--date] [--thread]`,等 flush 后逐话题打印记进日记了 / 没记成为什么)、**show**(`cotutor show <老师> <job> [日期] [--json] [--evals]`,2026-09-15:一轮的全部——就是 rawView 那份,文本摘要或 JSON 原样;`--evals` 看回放那轮;给 Claude Code 在 workspace 里分析用)、**replay / compare**(`cotutor replay <老师> <job> [日期] [--runtime] [--post]`:同一问按现在的 vault 与提示词再跑一遍,落 `evals/`,跑完并排打印;`cotutor compare <老师> [evalJob] [日期]` 再看 / 列;只有 CLI,家长端不露)、main
- `src/server/` — app(路由 `route()`,配置按 mtime 热重载;**作业照片**(R5,2026-09-14):`POST /api/kid/conversations/<老师>/photos` 与 `POST /api/conversations/<老师>/photos` 同一个 `uploadPhoto`(body `{image: data:image/jpeg|png;base64}`,解码后 ≤ 3 MB,`store.writeCapture` 落 `captures/<日期>/<HHMM>-<n>.jpg` 回 `{path}`;readBody 上限 6 MB),两个发消息接口都收 `photos[]`(`photosOf`:每个都得是 captures/ 里在的文件,否则 400;孩子端只带照片没有字也能发);`PUT /api/conversations/<老师>/<日期>/threads/<话题>/rating` 话题打星 {rating: 1–5 | null}、`POST …/<日期>/bookkeep` 记账 {threads?};`/api/kid/*` 是服务端过滤后的孩子视图(`today` / `<日期>` 带每条的 thread 与当前 thread,`history?days=` 按天列话题 `kidThreads`,POST 接 `newThread` / `thread`,卡状态的 turn = 该卡所在话题的末条);`PUT /api/kid/conversations/<老师>/cards/<job>/<n>` 存卡的状态,不起老师(canvas 带 image data URL 就落 png);`GET /api/kid/image?p=` 只给 workspace 根以内的图片;`GET /api/conversations/<老师>/<日期>/raw/<job>` 是看原文的六站(`/fixture` 出一份原文原样,家长端下载成 `tests/fixtures/board/` 的样本);`POST /api/tts/try` 跑一次 `tts.say` 回 data URL(设置页试一句);`/api/audio` 认 `.<n>.mp3` 与 `.cards/<n>/<k>.mp3`;`/stage/*` 舞台包与字体、`/api/bundles/<id>/<文件>` 课包,都在 `server/stage.ts`;**主屏幕**:`/manifest.webmanifest` 与 `/icon-{180,192,512}.png` 按 `title` 现生成(`lib/icon.ts`),孩子端 head 里 apple-* meta 齐,iPad 分享菜单「添加到主屏幕」进去就是全屏)、runner(发消息:拼上下文包(连同完整命令行落一份 `<日期>.<job>.run.json`——跑完就丢的话「老师为什么没看见孩子选了 C」永远查不了)(含上一轮之后改过的卡 `describeCard` 成 `cards:` 段;`photos` 原样进 `photos:` 段并记进消息,文字空就「(拍了一张)」/「(拍了 N 张)」)→ spawn / resume(stdout 经本进程落盘,同时喂 PartialReader → `parseBoard(partial)` → 内存里的 partial 板书,`runner.partial(tutor)` 给孩子端 pending 条目)→ 板书解析 → 配音收尾 → 索引物化(section / parentText / warnings / action / cards)→ 卡的资产(点读段)后台接着配到 `.cards/<n>/<k>.mp3`(`runner.flush()` 等它)→ 「## 转交」自动起目标老师的一轮(from: system,转交单 = 谁转的 + why + refs + 孩子的话 + voice;目标不在 / 关着 / 忙 / scene-maker 到了 `scenes.dailyMax` 不起,原因进 warnings,起了记 `handoffJob`;目标老师用自己的 `runtime`);**话题**(2026-09-11):`send` 的 `newThread` / from: system / 今天第一条开新话题(thread = 自己的 job、不 resume、不带旧卡),`thread` 指定接今天的旧话题(resume `sessionFor`),缺省接当前;**埋点**:每轮记 timing(首卡 = partial 第一次有卡、进程退出、配音收尾);**事件**(2026-09-13):spawn 里的 `emit` 在每道工序的关键点发一条 → 按序追加 `<日期>.<job>.events.jsonl` + 给 `runner.onEvent` 的订阅者(DubQueue 的 `report` 钩子报配音每条的排队 / 完成 / 失败;stdout 里含 `tool_use` 的行另扫一遍发工具事件,子代理标 sub;整块出的在 exit 后补发卡与句);消费者三个:`cotutor send` 现场逐行打印(`--lane` 过滤、`--quiet` 只要结果)、`serve --trace`、`cotutor trace <老师> <job> [日期]` 回放;流式 reparse 先 `parseSections` 剥固定段再 `parseBoard(partial)`(老师先写转交单那轮以前一张卡都出不来,靠控制台才看见);scene-maker 那轮收尾 `settleSceneLedger`:课包 id 取转交单 refs(没有就取收尾句「课包 x 做好了」),往 artifacts.jsonl 追加一行 costUsd / durationMs,老师忘了记账就补一整行(manifest 在 → ready,不在 → retired)并进 warnings,消息 artifacts 记 id;一老师同时一条,忙则 409;**上下文包** `gatherContext`:课程表 slot + 档案 profile(`extractProfile`)+ 本周计划 + 最近 14 天日记的观察行(`extractObservations`);可选第四参 `PackReport` 边取边记每段来自哪个文件、那里共几条、带了几条;`packDryRun` 用它做干跑(`cotutor pack <老师> [消息] [--from] [--at]`,2026-09-15:不起模型不写盘,改了档案 / 日记 / 计划立刻看老师会看见什么、截了什么);**记账** `bookkeep(tutor, date, only?)`:这天每个没记过、有孩子的话或打了分的话题各起一轮 `from: system` + `bookkeep {thread}`、resume 那个话题的会话(话题里有照片就把张数给 `bookkeepingPrompt`,summary 那行要求把册子 / 页 / 题号 / 题面写成文字、不写路径)(send 的 `date` / `thread` / `bookkeep` 三个新参数;没指定话题的系统消息仍开新话题),链在后台顺着跑、`flush` 等它;收尾 `settleDiary` 把「## 记账」段渲染成日记一段 `writeDiary` 追加、索引 `booked`,没段 / 对不上 / 空的进 warnings,事件 ledger:diary)、tts(`DubQueue` 一轮一条队三并行;`LineDubber` 讲稿每定稿一句就按 `tts.say` 合成到 `<日期>.<job>.<n>.mp3`,整轮跑完只等没配完的;失败句不响用浏览器的声)、store(索引 / 转录 / 卡的目录 `.cards/`(`scanCards`:状态 + 资产)/ cotutor.json 补丁的文件层;**`snapshotSources`**(2026-09-15)每轮起跑时把老师文件正文 + hash、`.claude/skills/*/SKILL.md` 的 hash 记进 run.json 的 `sources`——claude 走 `--agent` 正文不在命令行里,文件改了也查得回当时那份;`rateThread`;`writeCapture` / `capturePathOk` 作业照片的落盘与校验;vault 那半:`readDiaries` / `writeDiary`(update 回调,.tmp 再 rename)/ `readTextbooks`)、raw-view(**看原文**:一轮拆成几站——上下文包(带 sources 的 hash)/ **读了什么**(2026-09-15:消息 `tools`,老轮次从 .log 现抽;站 note 写几次工具 · Read 几个文件 · 几次失败)/ **时间线**(2026-09-13:读 events.jsonl,甘特的段与一行一条服务端算好 `timeline`,页面只画;每拍的埋点列在下面;老轮次没有事件 → none)/ 老师原文(逐行注解)/ 解析结果(拿当前解析器重解与索引里存的 diff)/ 下发给孩子(截掉的尾巴、剥完答案的 props)/ 配音 / 转录,全从盘上现读现算,不进索引;有卡的轮次第七站「板书后期」读 `.post.json`,带「再做一次」)、post(**板书后期**,2026-09-12;**2026-09-13 起按拍**,《工作流程.md》§三:一拍 = 一张卡 + 跟着它的讲稿句,拍关了(下一张卡出现)runner 就 `runBeatPost` 按 `policy.post.runtime`(出厂 `claude-fast`)spawn 一次快模型,提示词 `beatPrompt` 从**主题的 post.md 骨架**渲染(`themes/<主题>/post.md`,和 kid.css 同一套:出厂 / 拷贝 / hash / upgrade / 按 mtime 现读;任何一句都能改,占位符 `{rules}` `{output}` 与校验器 / schema 同源,`{cards}` `{lines}` `{context}` 这一拍与前 5 张已定的卡,`{tints}` `{looks}` `{pens}` `{defaultTint}` 从槽表拼;缺必需占位符退 `POST_TEMPLATE_FALLBACK`,doctor `theme.post` 点名),输出 `{row: same|new, look, marks[{line 拍内下标, card 缺省这拍, phrase, pen, said}], anchors}` 经 `validateBeatPost` 校验后套上(same 接上一行要上一行有位且两边都不独占,否则 new;标注只能标这拍或前面已定的卡;丢的记 dropped),提案存 decisions,partial 每次重建都重新套;回了 / 失败了都算 settled,拍的就绪等它;老师写完后没起过的拍并行补起,全部按卡的顺序套到定稿的节上,`assemblePost` 攒成 `.post.json`(version 2:beats[] + 汇总)与 `message.post` 摘要(加 beats / failed)、`timing.postMs`;`validatePost`(整节一次)保留,拆成各拍走同一个校验器(mock 的写死提案、repost);每拍超时 `post.timeoutMs`(出厂 10000)这拍素版不重来;**后期的子进程一律带 `MAX_THINKING_TOKENS=0`**(2026-09-13 真跑:claude CLI 缺省让 haiku 对一问一答的 JSON 想 3–6K token,一拍 35–69 秒、三拍全超时;关掉思考 4 秒、$0.015 一拍,`--effort low` / `--system-prompt` 都不管用,`--bare` 会没登录);**后期的 claude-fast 模板不发工具定义与技能索引**(2026-09-15:`--tools ""` + `--disable-slash-commands` + `--system-prompt 一句`,一拍输入 27K → 514 token,样本质量不变、p95 7.0s → 3.8s、费用 1/4;`--disallowedTools` 只禁调用、定义照发,数据在《快模型方案.md》§四);`repost` 拿老师原文重解再跑一遍改写索引(`cotutor repost`、`POST …/raw/<job>/repost`);消息记 `device`,缺省当平板横屏;坏了 = 素版,孩子端不知道这道工序存在)、parent-page(家长端 `/parent`:对话(**一轮一张卡**:问句 → 板书按 kind 渲染成卡片 → 埋点 chip 一行 → 孩子看到 → 待裁量 / 转交 / 出错三色通知块;顶栏与输入框各自钉死,`grid-rows auto/1fr/auto` + `min-height:0`,不再硬算 `100vh - 47px`;每个话题头上五颗星(再点同一颗清掉)与「已记进日记」,顶栏「记账」钮 POST bookkeep;转录与原文默认收着,右下角「看原文」开抽屉)/ 老师团(一位一行,九项政策折叠、改过的挂 chip 并高亮)/ 设置(路径 / 服务 / 配音三块,配音能「试一句」);输入区「📷 照片」钮(可多张,浏览器里缩到长边 1600 → 传 → 攒在 state.photos,发时带上、发前能去掉),每轮问句下缩略图点开看原图;调试 `?tutor=x`、`#chat?raw=<job>`)、kid-page(孩子端 `/`:**拍作业**(R5):相机 / 相册选了 → `shrink` 缩到长边 1600 的 jpeg → `POST …/photos` → `send('', {photos})`,节头 `.sh img.ph` 回显缩略图,pending 照旧;canvas 卡照片做底时紧凑态先是那张照片(`.th.base`),开舞台给 `imageUrl`;首页 + 老师页 = 板书页,照豆包爱学:一轮一节、卡先铺、讲稿逐句播、笔跟声标注、末句问句停下出「继续」;卡按 kind 渲染,紧凑态只读,点读段点了念(有 `<段号>.mp3` 资产放 mp3,没有浏览器合成);标注念到才画:`say` 的第三个回调在声音真开始时给总时长(mp3 取 duration、合成声按字数估),`markTiming` 算每处标注的时刻与描线时长(线条笔的 transition-duration 跟着词长),讲稿里没这个词的句首就画,念完没轮到的补上不描;**一拍一就绪**(2026-09-13,原来是整轮跑完才播):pending 条目第一拍就绪前什么都不铺(字幕「让我想想…」),就绪了 `renderLive` 把它转成 S.sections 里的 live 节开播,之后每次 poll 只铺新就绪的拍的卡(`placeCard`:按 `rowsFor` 落行,后期定了接上一行的进上一行的格子、列数按最终几张定,流式的 layout 只到已定的那几张(`rowsFor` 认前缀))、播到头 `thinking` 等下一拍;老师写完 `finalizeLive` 换成正式节不重播(没铺的卡补上、铺过的换样子、已播的标注补画;后期的排版这轮不用,播放中重排会跳;句的下标对不上按文字找回位置);运行出错 pending 消失 → live 节撤掉;runner 那头每句配音落盘就填进 partial 的 audio、重算 `ready`、发 ready:beat、记 `timing.firstReadyMs`(头号埋点,家长端 chip「首拍就绪」);点卡开**舞台**(盖住板书那块,讲稿暂停;选择题大按钮、填空逐空输入框、选了填了就 PUT 状态、「交给老师」= `{text:'', action:'submit', focus:{card}}`;图片舞台双指缩放;末句问句停下时锚点卡有交互就自动开);「继续」= `{text:'', action:'continue'}`;输入条「相机 | 发消息或按住说话 | 加号」;头部「以前的」(面板按天列话题:今天的旧话题切过去能接着聊,以前的日期只读回放——不 waiting、有 state 的卡不开、输入条换「回到今天」)与「新话题」(空白态,发第一句才真开;pending 时灰);一节 = 头一行(时间 · 节名)+ 若干行(`rowsFor`),流式时一行一张、转正整节重画;讲到哪张卡哪张 `.now`(舞台开着时舞台那张);转屏 / 改窗口按新端重排并重画标注;平板横屏板书居中最宽 1040、没有左栏;`?tutor=x&step=节.句` 调试停在某句,`&stage=节.卡` 直开舞台,`&device=端` 强制折行,`&new=1` / `&panel=1` / `&hist=日期.话题` 话题相关三态)、mock(`cotutor mock`:不经真实老师与配音的模拟接口,脚本就是老师会写的正文(过真解析器)+ 浏览器合成声,想的期间一张张露卡(模拟流式),卡的状态 PUT 假存,`/api/kid/image` 给占位 svg,`…/photos` 回假路径、消息收 photos,场景 normal / limit / offline,测前端交互与渲染,不需要 workspace)——两页都是内联脚本,只走 `/api/*`
- `skills/<name>/` — **本包的技能**(2026-09-15,包根,进 npm files;一技能一目录 SKILL.md + references/,frontmatter 只写 name / description,两 CLI 公共子集):现在两个:生成的 `cotutor-board`,和 **`cotutor-vault`**(2026-09-15:老师怎么读家长的 vault、什么时候读什么、记账段怎么回;SKILL.md 手写自《obsidian仓库设计.md》§7,`references/记账.md` 由 `src/lib/vault-doc.ts` 从 `BookkeepingEntrySchema` 的 `.describe()` 与真的 `bookkeepingPrompt` 生成——写法永远和解析器同源;老师文件尾巴与 planner 点名它(家规同日退出出厂);上下文包新增 `vault:` 段(root 绝对路径 + 各角色相对 root,`runner.vaultPack`),老师拼路径不用读 cotutor.json)。第三个 **`cotutor-analyze`**(2026-09-15,评估改动效果的收口:不是给老师的,是给**在 workspace 根开 Claude Code 的家长 / 开发者**的):SKILL.md 手写——老师只看得见上下文包与自己 Read 的、「为什么没提 X」的查法顺序、工具箱表(show / pack / trace 不花钱,replay 花钱先问)、六个常见问题的查法、禁区(不改 conversations / evals / ledger、不写 vault、不把 .claude/agents 的老师当子代理派);`references/命令与文件.md` 由 `src/lib/analyze-doc.ts` 从 `cli/usage.ts` 的用法文本(USAGE 为此从 main.ts 挪出来)与 `conversationFiles` 的命名规则生成,命令与文件名永远和代码同源;machine: true 出厂。第四个 **`cotutor-tune`**(2026-09-15 晚,和 analyze 对称:analyze 看、tune 改):家长在 workspace 根说「我想让数学老师…」「把孩子怕竖式记下来」「加一个英语老师」时用;SKILL.md 手写——铁律(每处改动先给 diff 点头才写、改完必验、不碰 conversations / evals / 机器件、改过的出厂老师 upgrade 只报 diff)、三条路子(改老师文件:先 show 现状、分清归属(口气规矩在文件,人设开关政策在 cotutor.json,关于孩子的事进档案),改法要小,replay 验;改 vault:只有档案「现在」callout / 日记「- 观察:」行 / 本周计划三处老师看得见,形状指向 cotutor-vault 不重写,pack 验;加老师:cotutor add 不手建,subject 要和课程表、日记学科一字不差,doctor + pack 验);`references/字段.md` 由 `src/lib/tune-doc.ts` 从 TutorSchema / PolicySchema 的 `.describe()` 与 POLICY_DEFAULTS、`AGENT_FRONTMATTER_KEYS`(agent-file.ts,为此抽成常量)、USAGE 生成,契约里没 describe 的字段表里就是空的(contextPack 三个为此补了 describe)。以后老师文件里三份相同的「看图」、转交段的写法也该抽成技能,老师文件只剩人设与学科。`scripts/gen-skills.ts` 的 `GENERATED` 表记哪些技能是整个生成、哪些只生成几个文件。`tests/skills.test.ts` 给每个出厂 SKILL.md 做 lint(frontmatter 能解析、键只许 name / description、name = 目录名、description ≤ 1536、围栏外行内反引号里不出现 `!` 与 `>字`——Claude Code 的 bash 权限检查会误判、技能装不上,hyperframes 踩过;drawtell 的四个只提醒不判失败,闸门在 drawtell 仓自己的 tests/skills.test.ts)
- `tests/*.test.ts` — 每文件一子进程(`scripts/test.ts`),零依赖 `check()`;HOME 注入后动态 import 的手法同 drawtell。`tests/_fake-cli.ts` 是假 CLI(模仿 stream-json、认 `--resume`,`--stream` 按行吐 delta 模仿 `--include-partial-messages`;上下文包有 `photos:` 段就 Read 那张、出 image 卡 + 照片做底的 canvas 卡),`_fake-tts.ts` 是假配音命令,runner 全流程测试靠它们,不花钱

## 运行与验证

- `pnpm typecheck`;`pnpm test`;`node bin/cotutor.js --help`(bin 直跑 src,Node ≥ 22.18);改了 `cards/*/card.md` 跑 `pnpm run gen:skills` 重生成 `skills/cotutor-board/`(不跑 skills.test 会红);`pnpm run build:stage` 打舞台包(改 src/stage 后要重打,dist/stage 不在 git 里;没打包时重卡舞台开不了、doctor `stage.bundle` 点名)
- 改解析器或卡先过 `tests/board.test.ts`(真跑样本在 `tests/fixtures/board/`);改页面模板注意反斜杠要写成 `\\n`、正则里的 `\/` 要写成 `\\/`(模板字符串会吃一层;`\/` 变 `/` 会把那行变成 `//` 注释而脚本照样能解析),mock.test 有「内联脚本能解析」「正则斜杠没被吃掉」兜着
- 舞台里的东西(播放器、excalidraw)用 Chrome DevTools 协议驱动最可靠(`--remote-debugging-port` + WebSocket,Runtime.evaluate 进 iframe 的 contentDocument;画板用 iframe 内的 PointerEvent 画,顶层 Input.dispatchMouseEvent 画不上);`Page.captureScreenshot` 在这种无头模式下会卡,截图用 CLI `--screenshot`
- 作业照片线的手动验收 `node scripts/probe-photo.mjs`(2026-09-14):自己起临时 workspace(假 CLI `--stream`、后期关、端口 8792)+ HTTP serve,把服务画的 `/icon-192.png` 当照片传上去、只带照片发一条,CDP 开孩子端查节头缩略图、直开画板舞台查 excalidraw 场景里的底图(`window.__excalidraw`)、画一笔交回去 png 落盘、家长端问句下缩略图,10 项;`--keep` 留下 workspace 与 serve。无头 Chrome 的 `--screenshot` 对着这个真 serve:孩子端截到了(`--virtual-time-budget=8000`),`/parent` 会挂住(`--timeout` / `--virtual-time-budget` 都不解,进程要 pkill),家长端要看样子用 CDP 探针里的 DOM 查验代替
- 声音路径的手动验收 `node scripts/probe-thread-voice.mjs`(2026-09-12):开一个**看得见的** Chrome(不 headless,`--autoplay-policy=no-user-gesture-required` 出声),CDP 在输入条打字发一句,`Fetch` 域扣住第一段 mp3 请求让 `play()` 停在 pending,这时点「新话题」,看合成声有没有被叫起来念旧话题那句;要一个跑着的 serve:临时 workspace 的 runtimes 指 `tests/_fake-cli.ts --stream`(老师免费)、`tts.say` 走真 voxtell(出真 mp3;假配音写的不是音频,浏览器直接 onerror,走不到那条路)、`policyDefaults.post.mode` off。当时的 bug:stopVoice 的 pause 让 pending 的 play() 以 AbortError 拒掉,catch 当成「配音放不出来」退回合成声,现在退回前查 voiceToken
- 前端单测不花钱:`node bin/cotutor.js mock`(或 `--scenario limit|offline`)起模拟接口,浏览器开 `/?tutor=chinese-tutor&step=0.3`;无头 Chrome 截图手机尺寸要 `--force-device-scale-factor=2 --window-size=780,1688`(窗口最小宽约 500,直接 390 会被裁),平板 `--window-size=1180,820`
- 冒烟:`node bin/cotutor.js init <slug> --dir <tmp>` → `doctor --workspace <tmp> --live`(真起一次老师)→ `serve --workspace <tmp>` → `curl /api/tutors?kid=1`;真跑老师:`cotutor send math-tutor "<消息>" --workspace <tmp>`(或页面 `/parent`)。手册三份:docs/家长手册.md(只用)、docs/开发者手册.md(改代码)、docs/iPad与iPhone.md(证书 / 信任开关 / 用 IP 不用 .local / 加到主屏幕,分设备各做一次)
- **老师会话与本机 claude 配置隔离**(2026-09-15):三个 claude 运行时模板都带 `--setting-sources project`,只读 workspace 的 `.claude/`;实测不加时 `~/.claude/skills/` 的 obsidian-cli / officecli、`additionalDirectories`、peon-ping hooks 全进老师会话。代价:`~/.claude/settings.json` 的 `env`(代理)也不进,没代理直接 403——serve 要从 export 了代理的 shell 起,doctor `env.userSettings` 点名、`--live` 的 403 指南说这条。cwd 向上的 CLAUDE.md 链不受这个旗标管(`--bare` 才关,但它不认登录),链上现在一份都没有。`--restricted` 能把文件工具圈在工作目录里但拒绝 bypassPermissions,无头跑不了
- 在 Claude Code 会话里起 claude 子进程要 `env -u CLAUDECODE ...`(嵌套会拒);本机 claude 2.1.220 不认全局 settings 里的模型,运行时模板加 `--model sonnet` 才能跑(模型旋钮本来就在模板里);流式靠模板里的 `--include-partial-messages`(出厂模板有,老 workspace 的 cotutor.json 要自己加,不加就整块出),qwen 0.21.13 没有对应开关
- voxtell 没发 npm:从 TarsLab/voxtell 检出后 `pnpm add -g .` 进 PATH(pnpm ≥ 10 没有 `link --global`;首次先 `pnpm setup` 把 `~/Library/pnpm/bin` 加进 PATH 并重开终端),`tts.say` 出厂值就能用;不链就把 `tts.say[0..1]` 改成 `node <voxtell 检出目录>/bin/voxtell.js`;iPad 真机要 `cotutor cert`(mkcert)+ 把根证书装到 iPad

## 约定

- **证书是机器级的**(2026-09-09 拍板):签的是本机名与局域网 IP,与孩子无关,所以放 `~/.config/cotutor/certs/`(`USER_CERT_DIR`,与 `config.json` 同目录),不在 workspace 里;多孩子共用一张,IP 变了只重签一次
- **workspace 解析链**:`--workspace` > `COTUTOR_WORKSPACE` > cwd 或祖先有 cotutor.json > `~/.config/cotutor/config.json` > `~/cotutor/` 下唯一的孩子目录 > 报错附修复指南。没有 cwd 兜底,cotutor.json 是必需的政策文件;在但坏了响亮报错
- **init 幂等补缺**,已有文件一律不动;政策文件(cotutor.json)只在缺失时写模板;根的 CLAUDE.md / QWEN.md 是家长自建的,机器不写不查。唯一例外:`.claude/agents/` 里指向包的旧链会被换成拷贝(链不是用户数据)
- **老师按 cotutor.json 里有谁走,没有注册表**:init / doctor 对表里每一位补目录与 `.qwen` 链、查文件;出厂五位多一层 hash 与 upgrade,自家加的(`cotutor add <name> --display …`:出模板文件、`patchConfig` 进表、建目录)永远是 untracked,upgrade 不碰,doctor 的 origin 行写「自家加的老师」
- **老师文件与 cotutor.json 的字段归属**:文件里只有运行字段 + 正文;display / avatar / voice / enabled / hidden / policy / runtime 全在 cotutor.json。policy 的 `board: auto | off` 决定要不要出卡,off 时上下文包带 `board: off`;`scenes.dailyMax`(缺省 2)= 每天最多起几个场景作业(一个 ≈ 一轮问答的 30 倍费用)。**已有 workspace 的 cotutor.json 不会自动多出新出厂件**(政策文件不动):差异由 `src/cli/migrate.ts` **现算**(拿这份自己的 slug 生成一份出厂模板去比),doctor 的 `config.migrate` 点名、`cotutor upgrade --config [--dry-run]` 与家长端设置页顶上的卡只**补缺**(新老师连文件与家一起补,命令模板里缺的旗标按出厂位置插进去),家长写过的值一个不动
- **给 scene-maker 的转交,课包 id 三处一致**:runner 起作业前 `settleBundleId`——refs 里没有合法 id 就取这节里 scene 卡的 bundle;scenes/<id>.ts|.md 或 bundles/<id>/ 已占用就改成 `<id>-2`,卡、refs、转交单一起改(2026-09-10 真跑:老师两次起了同一个 id,孩子卡上放了旧课包)
- **转交只写「## 转交」段,不派子代理**:claude 会把 `.claude/agents/` 里的老师文件当可派的子代理,老师自己 Task 派 scene-maker 会在自己那轮里烧自己的预算(2026-09-10 真跑见过);三位学科老师文件都写了这条
- **孩子视图是机械规则**(2026-09-10 改板书):正文 = `kidSource`(这轮里**带围栏或带 H2 固定段的顶层文本段 + 最后一段**,顺序不变;`result.result` 只是最后一段,老师板书完再用工具、再补一句时板书不丢——真跑见过三次才定的规则;流式的 PartialReader 同一条),剥掉「## 待裁量」「## 转交」段之后,**正文就是板书**——普通段落是讲稿(一行一句,念出来),围栏是卡(标签 = kind);第一个 H2 起(「## 家长」)是给家长的尾巴,孩子看不到;kidText = 讲稿各句拼起来,每句按 replyMaxChars 截;答案(choice 的 `[x]`、fill 的 `= 答案`)下发前 `stripSecrets`。老师的写法在 agents/*.md 与家规里,真跑样本在 `tests/fixtures/board/`(解析器的回归样本,改解析器先过它们);这两种固定段只吃字段行,遇空行后不是字段行就结束
- **卡的状态是孩子的,存服务端**(2026-09-10):`conversations/<老师>/<日期>.<job>.cards/<n>.json` = `{at, turn, state}`,同目录 `<n>/<k>.mp3` 是后台生成的资产(点读段),turn 是存的时候索引末条的 job;发消息时 turn 等于当时末条的卡就是「上一轮之后改过的」,逐张 `describeCard` 进上下文包 `cards:` 段(带答案,老师口头说对错),消息记 `cards[]`,家长视图显示「孩子在板书上做的」;孩子端下发时并到 `section.cards[n].state`,重开页面选中态还在。状态文件不进索引、不需要重建
- **作业照片在学科老师这里拍,落 workspace 不落 vault**(R5,2026-09-14,《产品规划.md》拍板 14):照片是 `captures/<日期>/<HHMM>-<n>.jpg`(`paths.captures`,相对根;`.gitignore` 了),消息与上下文包 `photos[]` 原样给老师 Read(老师 cwd 是 agents/<name>/,所以要加 `../../`,老师文件里写了);老师认不出在讲稿里让孩子重拍,不出待裁量;`/api/kid/image?p=` 只给根以内的图,`paths.captures` 挪到根外页面就看不到(doctor 点名);vault 里只留记账时认出的文字,`diaryTopic` 不带 photos、日记永不写 `captures/` 路径;作业老师退出主路(出厂关着,家长端能开)
- **vault 是长期记忆,机器写它只有三处**(2026-09-14 拍板,《obsidian仓库设计.md》):日记追加(记账)、计划新建、教材缺失时建骨架;永不写 `#标签`、永不写图片(照片落 workspace `captures/`,vault 里只存老师认出的文字)、永不改家长写过的字。老师(LLM)不直接写 vault:记账回「## 记账」段,应用渲染。观察的真相在日记的「- 观察:」行(上下文包 recent 从最近 14 天抽),`observations.jsonl` 退役。**代码不认题型**:唯一的锚点是教材单元标题 `[[册#节]]`,分类归家长在 `参考/` 自建、在教材口径里链。打分的单位是话题,≥ `vault.keepScore` 记账时摘要与骨架才进日记,孩子问的话总是进
- **产物账本追加式**,同 id 后者为准;坏行 doctor 点行号,不删账本
- **对话索引物化**:`conversations/<老师>/<日期>.json` 每条消息带 kidText / holdup / handoff / agent(运行时名)/ error,页面不解析日志;家长视图的转录行从 `.log` 现读(`foldRuns`,`sub` 行折成「子代理」)。换运行时(agent 不同)→ 新开会话且索引里的 session 换成新的;跨天 → 新文件自然不 resume
- **孩子端永远没有错误与评判**:`/api/kid/*` 只给问句 / 回复 / 配音 / pending(出错的运行不出现,家长的问句不露),每日上限 `dailyMessages` 只数孩子发的(「继续」不算,交答案算),到了 429 且首页头像灰;卡上不画对错,对错只在老师嘴里;页面上任何失败都只是「什么都不出现」或头像灰(后端不通 → `offline`),没有报错文案。识别在浏览器(webkitSpeechRecognition zh-CN 单句:按住 150ms 起、松手 stop 后 onend 直接发、上滑 60px abort;没有构造器 / 非 HTTPS / 没麦克风权限就只剩打字,服务端没有语音接口、不存声音),配音在服务端(没有退回 speechSynthesis)
- **cotutor.json 在页面上能改**:title / policyDefaults / tutors / runtimes.default / paths / server / tts,补丁深合并到原始 JSON(保留 `$schema` `_note`),整份过契约才落盘,null 删键;kid / version / 运行时模板走编辑器。老师文件正文走 `/api/tutors/<name>/file`(frontmatter name 必须不变),新老师 `POST /api/tutors` 与 `cotutor add` 同一条路,`DELETE` 只删自家的(文件改名保留)。服务每个请求看 mtime 热重载,改坏了留旧配置、`/api/health` 报 configError;**热的只有 cotutor.json**,页面与 runner 是代码,改了 `src/` 要重起 `serve`(老师文件每轮现读,不用)
- 两 CLI(2026-09-08 实测):claude 用 `--agent` 让老师文件当主会话(链上的定义能被找到,`memory: project` 落在老师目录);qwen 0.21.13 **没有** `--agent`(Unknown argument),但 `--append-system-prompt` 有且生效、`--approval-mode yolo` / `--yolo` / `--max-wall-time` / `--resume` 都在,stream-json 事件带 `parent_tool_use_id`——老师正文由应用读文件填进预设占位 `{agentBody}`
