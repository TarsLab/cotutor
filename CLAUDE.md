# cotutor

家里的 AI 老师团,和家长一起教(应用仓,全新项目,2026-09-08 起)。设计文档在 `docs/`:《产品规划.md》(定位、两个端、回复形式阶梯、里程碑 R0–R8、拍板记录)、《agent层设计.md》(目录、三种记忆、子代理、规划老师)、《契约草案.md》(各文件形状,已逐条拍板;契约的真相是 `src/schema/`,它只剩「为什么」)。拍板都在那里;本文件只写这个仓怎么长的。草稿与随手记放 `docs/wip/`(gitignore,不入库)。底层工具 drawtell / voxtell 按 npm 版本依赖,不在本仓(2026-09-10 起 drawtell、drawtell-skills 还没发 npm,package.json 里暂时是 `link:../drawtell` `link:../drawtell-skills`,发 cotutor 前必须换成版本号)。

## 结构(单包,subpath export,同 drawtell)

- `agents/*.md` — **老师文件**(frontmatter 取两 CLI 公共子集:name / description / maxTurns / permissionMode / memory,正文即系统提示)。出厂六位:math / chinese / reading / homework / planner(hidden)/ scene-maker(hidden,画图老师:别的老师「## 转交」一道题过来,它用 drawtell 做课包到 bundles/<id>/,工作流写在正文,点名四个领域 skill;runtime 用出厂的 `claude-scene`,预算 8 美元)。是出厂件:init **拷贝**进 workspace 的 `.claude/agents/`(2026-09-09 拍板,原为链),出厂 hash 记 `.cotutor/shipped.json`,`cotutor upgrade` 据此换新或报 diff(`src/cli/tutors.ts`);人设、政策、开关不在这里,在 workspace 的 cotutor.json
- `themes/default/` — **出厂主题**(孩子端板书的样子):`theme.json` 是槽的清单(tints 底色槽 / looks 字形槽 / pens 五支笔,每槽一个名字 + 一句「给什么用」,槽任意多,`default` 必须在 tints 里),`kid.css` 是值与样式(板、卡、笔、舞台壳;页面骨架仍在 kid-page.ts)。是出厂件:init **拷贝**进 workspace 的 `themes/default/`,目录 hash 记 `.cotutor/shipped.json` 的 themes,`cotutor upgrade` 同一套(`src/cli/themes.ts`);`cotutor add-theme <name>` 拷一份来改,cotutor.json `kid.theme` 指过去;服务 `/kid/theme.css` `/kid/theme.json` 按 mtime 现读(`src/server/theme.ts`),读不到 / 坏了退包里的 default,doctor `theme.manifest` 点名;舞台包的 index 也 link 它。契约 `src/schema/theme.ts`。2026-09-12 样子按原型落地:板 `#faf9f4`,卡 `.c[data-tint=<槽>]` 底色 + 同系 1.5px 描边、`data-look=<槽>` 字形,选中态 `.c.now` 主色描边 + 外晕;面类笔(marker / tint)是 span 背景,线条类(underline / box / circle)是贴在卡上的 SVG 路径(`.pen`),念到时 `stroke-dashoffset` 描出来
- `src/cards/` — **卡的注册表**:每种卡一个文件(props zod、围栏正文 `parse`、`strip` 剥答案、有交互的再加 `state` zod 与 `describe(props, state)` 给老师的一句、要预生成配音的再加 `assets(props)`、`doc` 语法表片段),`index.ts` 登记 + `parseCard`(不认识的标签当 code 卡,解析失败退 text 卡 + warning,永不抛)+ `stripSecrets`(答案剥掉、孩子的 state 照给)+ `parseCardState` / `describeCard` / `cardLabel` / `cardAssets` + `boardSyntaxDoc`(生成给老师看的语法表);现有 text(修饰 cover / note / quote / formula / step)、read(assets:一段一个 `<k>.mp3`)、choice(state `{picked[]}`,多个 `[x]` → `multi`)、fill(state `{answers[]}`)、image(相对 workspace 的路径或 http 地址 + 图注)、scene(正文课包 id;title / problem / steps / ready / thumb 是下发时 `server/scene-props.ts` 从 bundles/<id>/ 现读的快照;state `{step, done}`)、canvas(底图课包 id / 行内骨架 / 空白 + 题目;state `{ink[], image?}`,png 由 PUT 时服务端落 `.cards/<n>.png`)、code。页面那一半在 kid-page.ts(紧凑态 + 舞台态渲染)与 kid-board.ts(标注落点、`hasState` / `cardTitle` / `togglePick` / `pickedLabels` / `filledAnswers` / `stateSummary` / `segmentAudio`),按 kind 名对上;加一种卡 = 加一个文件 + 一行登记 + 页面一个分支
- `src/schema/` — **契约**(zod,类型即文档;`explainIssues` 把 issue 变修复指南):config(cotutor.json,含 runtimes / tts与 server.https,字段带 `.describe()`)、context-pack、conversation(消息带 section / parentText / timing:startedAt + firstCardMs / doneMs / dubbedMs,从进程起算的毫秒;`thread` = 话题 id = 话题首条的 job,索引 `sessions[thread]` 每个话题自己的会话、顶层 `session` 只是当前话题的;旧索引缺这两样按 `threads()` 现算)、board(板书节:卡 {kind, props} + 讲稿句;类型真相在 lib/kid-board.ts,zod 对齐)、ledger(产物行可带 costUsd / durationMs)、sections(待裁量 / 转交;`lineMap` 把 body 行号映回原文)、plan、timetable、json-schema(`cotutorJsonSchema()` 从 zod 现生成,init / upgrade 写到 workspace 的 `.cotutor/cotutor.schema.json`,cotutor.json 首行 `$schema` 指过去)
- `src/lib/` — **纯函数**(全部离屏可测):agent-file、sections、transcript(stream-json → 家长条目 + 最终文本,子代理事件标 sub)、board(**板书解析器** `parseBoard`:正文里围栏 = 卡、普通行 = 讲稿一句、第一个 H2 起是给家长的尾巴;`[词]` 标注整节找、`[[名 参数]]` 是对上一张卡的 cue、句子锚到上一张卡;`partial` 模式压住没闭合的围栏与没换行的末行,给流式用;还返回 `spans`(每张卡 / 每句是原文哪几行)与带 `line` 的 warning,`annotateSource` 据此把老师原文逐行标上「解析器把它当什么了」——它真跑 parseSections + parseBoard 再把行号映回原文,不另写一套,所以不会和真解析漂开)、kid-view(孩子视图:剥段 → parseBoard → kidText = 讲稿各句拼起来、每句按 replyMaxChars 截;`kidConversation` 把索引过滤成孩子端条目并 `stripSecrets`)、kid-board(板书的 JSON 契约 `BoardSection` + 孩子端全部交互逻辑:标注锚点、播放状态机、字幕行、输入条;**不能有运行时 import**,kid-page 把它剥掉类型内联进页面,两处一份源码;2026-09-12 加:卡的 `look {tint, look, emoji}`、标注的 `pen`、节的 `layout {for, rows}` 三个可选字段(板书后期填,没有走机械规则 `tintFor` / `lookFor` / `penFor`);`rowsFor(section, device)` 按端折行(同端照排、手机上两张都短才并排、标题行与有状态的卡独占)、`deviceFor(w, h)`、`nowCard` 选中态、`penBox` / `penPath` 线条笔的 SVG 路径(种子取词的 hash);text 卡 `heading: true` 是小节标题行,不算卡)、stream(`createPartialReader`:从 stream-json 增量里拼当前顶层回复,claude 的 delta 与 qwen 的整条 assistant 都认,子代理跳过)、context-pack(`focus.card`、`cards:` 段)、plan、ledger、conversation(文件命名含 `.cards/<n>.json`;`threads` / `currentThread` / `sessionFor` / `lastJobOf` 话题的纯规则:第一条与 from: system 各开一个、其余跟前一条;`changedCards(index, states, thread)` 挑这个话题里上一轮之后改过的卡;`applyRun` 按话题记会话,话题首条一律新会话)、timetable(抄 growth-apps,孩子列可选)、icon(主屏幕图标与 manifest:PNG 按尺寸**现画**——没字体可用,所以是纯几何标记,3 倍超采样 + 自己编 PNG(只 node:zlib),同尺寸缓存;`webManifest(title)` 是 standalone 清单)
- `src/lib/run-plan.ts` — 一次运行的命令规划:同运行时有会话 → resume,否则新开;`{agentBody}` 只给模板里用到它的运行时
- `src/stage/` — **舞台包**(React):重卡(scene / canvas)的播放器与编辑器,`scripts/build-stage.mjs` 用 esbuild 打成 `dist/stage/`(**splitting: true**,2026-09-11:整包 8MB 但 mermaid / cytoscape / katex 是 excalidraw 自己 `import()` 懒加载的,不拆会被全部内联进一个文件;拆开后 stage.js 21K + 静态 chunk 共 1.4MB,真开一次场景舞台下 3.4MB、画板舞台 1.5MB,拆包前两者都是 8.6MB。画板编辑器再由 main.tsx 的 `lazy(() => import('./canvas.tsx'))` 单独成块。alias 把 react / react-dom / excalidraw 钉到本包 node_modules,免得 link 进来的 drawtell 带第二份 React;excalidraw 的 css 按 `dist/prod/index.css` 引,CSS 不拆),`pnpm run build:stage`,prepublishOnly 也跑;孩子端页面在 iframe 里装 `/stage/?card=<id>`,postMessage 协议在 `protocol.ts`(card / control ↔ ready / phase / state / submit / close / error);`?bundle=<课包 URL>&autoplay=1` 不用页面也能开。scene.tsx = drawtell 的 ChalkPlayer(controls 关,stepIndex 0 起、对外报 1 起),canvas.tsx = 画板工作台(2026-09-12):excalidraw 只当画布,它的工具栏 / 菜单 / 缩放 / 右键菜单全用 stage.css 藏掉,工具是自己的七个钮(笔 / 橡皮 / 黑红蓝 / 撤销 = 抹孩子最后一笔 / 清空要点两下),竖屏一行、横屏一列(纯 CSS 媒体查询);题目条可收起;底层 locked + layer base + opacity 45 灰一档,孩子的笔 layer ink;「给老师看」exportToBlob 出 png 时底图恢复原色;页面的舞台顶栏对画板只写「画一画」,动作条左边「画了 N 笔」;交过之后紧凑态是缩略图(真服务 png 路径,mock data URL);手动验收 `node scripts/probe-canvas.mjs`(CDP:iframe 里 PointerEvent 画三笔 → 撤销 → 给老师看 → 缩略图))。tsconfig 有 jsx + DOM lib,tsconfig.build 排除它
- `src/cli/` — workspace(解析链 + 校验)、migrate(老 workspace 的配置迁移:`configGaps` 现算差异、`upgradeConfig` 只补缺)、skeleton(骨架清单 + 模板,init 与 doctor 共用;`writeSyntaxFile` 把注册表生成的板书语法表写到 `.cotutor/板书语法.md`,机器文件,init / upgrade 每次刷新,老师文件让老师讲解前读它)、tutors(老师文件拷贝 / 状态 / upgrade / add 模板 / 页面读写删)、skills(drawtell-skills 的四个领域 skill 拷进 `.claude/skills/`,同一套 hash / upgrade 机制,`.qwen/skills/` 相对链;`.cotutor/drawtell` 壳脚本指向本包 node_modules 的 drawtell,机器文件)、themes(出厂主题拷贝 / 状态 / upgrade / add-theme;`readManifest` 现在保留 skills 与 themes 两段,以前 tutors 一写就把 skills 抹了,靠顺序碰巧没出事)、init、doctor(`--live` 真起一次老师与配音,`explainLlmFailure` 把 API 层错误变修复指南;`env.nested` 认 CLAUDECODE)、upgrade、add、serve(机器级 `~/.config/cotutor/certs/` 有证书就 HTTPS,`server.https` 是单 workspace 例外)、cert(mkcert 签到 `~/.config/cotutor/certs/`,不需要 workspace)、send(终端发一条,与页面同一条路)、main
- `src/server/` — app(路由 `route()`,配置按 mtime 热重载;`/api/kid/*` 是服务端过滤后的孩子视图(`today` / `<日期>` 带每条的 thread 与当前 thread,`history?days=` 按天列话题 `kidThreads`,POST 接 `newThread` / `thread`,卡状态的 turn = 该卡所在话题的末条);`PUT /api/kid/conversations/<老师>/cards/<job>/<n>` 存卡的状态,不起老师(canvas 带 image data URL 就落 png);`GET /api/kid/image?p=` 只给 workspace 根以内的图片;`GET /api/conversations/<老师>/<日期>/raw/<job>` 是看原文的六站(`/fixture` 出一份原文原样,家长端下载成 `tests/fixtures/board/` 的样本);`POST /api/tts/try` 跑一次 `tts.say` 回 data URL(设置页试一句);`/api/audio` 认 `.<n>.mp3` 与 `.cards/<n>/<k>.mp3`;`/stage/*` 舞台包与字体、`/api/bundles/<id>/<文件>` 课包,都在 `server/stage.ts`;**主屏幕**:`/manifest.webmanifest` 与 `/icon-{180,192,512}.png` 按 `title` 现生成(`lib/icon.ts`),孩子端 head 里 apple-* meta 齐,iPad 分享菜单「添加到主屏幕」进去就是全屏)、runner(发消息:拼上下文包(连同完整命令行落一份 `<日期>.<job>.run.json`——跑完就丢的话「老师为什么没看见孩子选了 C」永远查不了)(含上一轮之后改过的卡 `describeCard` 成 `cards:` 段)→ spawn / resume(stdout 经本进程落盘,同时喂 PartialReader → `parseBoard(partial)` → 内存里的 partial 板书,`runner.partial(tutor)` 给孩子端 pending 条目)→ 板书解析 → 配音收尾 → 索引物化(section / parentText / warnings / action / cards)→ 卡的资产(点读段)后台接着配到 `.cards/<n>/<k>.mp3`(`runner.flush()` 等它)→ 「## 转交」自动起目标老师的一轮(from: system,转交单 = 谁转的 + why + refs + 孩子的话 + voice;目标不在 / 关着 / 忙 / scene-maker 到了 `scenes.dailyMax` 不起,原因进 warnings,起了记 `handoffJob`;目标老师用自己的 `runtime`);**话题**(2026-09-11):`send` 的 `newThread` / from: system / 今天第一条开新话题(thread = 自己的 job、不 resume、不带旧卡),`thread` 指定接今天的旧话题(resume `sessionFor`),缺省接当前;**埋点**:每轮记 timing(首卡 = partial 第一次有卡、进程退出、配音收尾);scene-maker 那轮收尾 `settleSceneLedger`:课包 id 取转交单 refs(没有就取收尾句「课包 x 做好了」),往 artifacts.jsonl 追加一行 costUsd / durationMs,老师忘了记账就补一整行(manifest 在 → ready,不在 → retired)并进 warnings,消息 artifacts 记 id;一老师同时一条,忙则 409)、tts(`DubQueue` 一轮一条队三并行;`LineDubber` 讲稿每定稿一句就按 `tts.say` 合成到 `<日期>.<job>.<n>.mp3`,整轮跑完只等没配完的;失败句不响用浏览器的声)、store(索引 / 转录 / 卡的目录 `.cards/`(`scanCards`:状态 + 资产)/ cotutor.json 补丁的文件层)、raw-view(**看原文**:一轮拆成六站——上下文包 / 老师原文(逐行注解)/ 解析结果(拿当前解析器重解与索引里存的 diff)/ 下发给孩子(截掉的尾巴、剥完答案的 props)/ 配音 / 转录,全从盘上现读现算,不进索引;有卡的轮次第七站「板书后期」读 `.post.json`,带「再做一次」)、post(**板书后期**,2026-09-12,《卡片重设计评估.md》§五 §六:`runPost` 按 `policy.post.runtime`(出厂 `claude-fast`)spawn 快模型,提示词由 `lib/postprocess.ts` 从主题清单现拼,输出 `{marks, anchors, layout, look}` 经 `validatePost` 校验后套在这节上(丢的记 dropped),等 `post.timeoutMs` 超时先素版;runner 里有卡就起、与配音收尾并行、结果进索引 `section`(marks 带 pen / cards 带 look / layout)与 `message.post` 摘要、`timing.postMs`;`repost` 拿老师原文重解再跑一遍改写索引(`cotutor repost`、`POST …/raw/<job>/repost`);消息记 `device`,缺省当平板横屏;坏了 = 素版,孩子端不知道这道工序存在)、parent-page(家长端 `/parent`:对话(**一轮一张卡**:问句 → 板书按 kind 渲染成卡片 → 埋点 chip 一行 → 孩子看到 → 待裁量 / 转交 / 出错三色通知块;顶栏与输入框各自钉死,`grid-rows auto/1fr/auto` + `min-height:0`,不再硬算 `100vh - 47px`;转录与原文默认收着,右下角「看原文」开抽屉)/ 老师团(一位一行,九项政策折叠、改过的挂 chip 并高亮)/ 设置(路径 / 服务 / 配音三块,配音能「试一句」);调试 `?tutor=x`、`#chat?raw=<job>`)、kid-page(孩子端 `/`:首页 + 老师页 = 板书页,照豆包爱学:一轮一节、卡先铺、讲稿逐句播、笔跟声标注、末句问句停下出「继续」;卡按 kind 渲染,紧凑态只读,点读段点了念(有 `<段号>.mp3` 资产放 mp3,没有浏览器合成);流式时 pending 条目的 partial 板书按下标只追加卡、不播,跑完那节转正再从第一句播;点卡开**舞台**(盖住板书那块,讲稿暂停;选择题大按钮、填空逐空输入框、选了填了就 PUT 状态、「交给老师」= `{text:'', action:'submit', focus:{card}}`;图片舞台双指缩放;末句问句停下时锚点卡有交互就自动开);「继续」= `{text:'', action:'continue'}`;输入条「相机 | 发消息或按住说话 | 加号」;头部「以前的」(面板按天列话题:今天的旧话题切过去能接着聊,以前的日期只读回放——不 waiting、有 state 的卡不开、输入条换「回到今天」)与「新话题」(空白态,发第一句才真开;pending 时灰);一节 = 头一行(时间 · 节名)+ 若干行(`rowsFor`),流式时一行一张、转正整节重画;讲到哪张卡哪张 `.now`(舞台开着时舞台那张);转屏 / 改窗口按新端重排并重画标注;平板横屏板书居中最宽 1040、没有左栏;`?tutor=x&step=节.句` 调试停在某句,`&stage=节.卡` 直开舞台,`&device=端` 强制折行,`&new=1` / `&panel=1` / `&hist=日期.话题` 话题相关三态)、mock(`cotutor mock`:不经真实老师与配音的模拟接口,脚本就是老师会写的正文(过真解析器)+ 浏览器合成声,想的期间一张张露卡(模拟流式),卡的状态 PUT 假存,`/api/kid/image` 给占位 svg,场景 normal / limit / offline,测前端交互与渲染,不需要 workspace)——两页都是内联脚本,只走 `/api/*`
- `tests/*.test.ts` — 每文件一子进程(`scripts/test.ts`),零依赖 `check()`;HOME 注入后动态 import 的手法同 drawtell。`tests/_fake-cli.ts` 是假 CLI(模仿 stream-json、认 `--resume`,`--stream` 按行吐 delta 模仿 `--include-partial-messages`),`_fake-tts.ts` 是假配音命令,runner 全流程测试靠它们,不花钱

## 运行与验证

- `pnpm typecheck`;`pnpm test`;`node bin/cotutor.js --help`(bin 直跑 src,Node ≥ 22.18);`pnpm run build:stage` 打舞台包(改 src/stage 后要重打,dist/stage 不在 git 里;没打包时重卡舞台开不了、doctor `stage.bundle` 点名)
- 改解析器或卡先过 `tests/board.test.ts`(真跑样本在 `tests/fixtures/board/`);改页面模板注意反斜杠要写成 `\\n`、正则里的 `\/` 要写成 `\\/`(模板字符串会吃一层;`\/` 变 `/` 会把那行变成 `//` 注释而脚本照样能解析),mock.test 有「内联脚本能解析」「正则斜杠没被吃掉」兜着
- 舞台里的东西(播放器、excalidraw)用 Chrome DevTools 协议驱动最可靠(`--remote-debugging-port` + WebSocket,Runtime.evaluate 进 iframe 的 contentDocument;画板用 iframe 内的 PointerEvent 画,顶层 Input.dispatchMouseEvent 画不上);`Page.captureScreenshot` 在这种无头模式下会卡,截图用 CLI `--screenshot`
- 前端单测不花钱:`node bin/cotutor.js mock`(或 `--scenario limit|offline`)起模拟接口,浏览器开 `/?tutor=chinese-tutor&step=0.3`;无头 Chrome 截图手机尺寸要 `--force-device-scale-factor=2 --window-size=780,1688`(窗口最小宽约 500,直接 390 会被裁),平板 `--window-size=1180,820`
- 冒烟:`node bin/cotutor.js init <slug> --dir <tmp>` → `doctor --workspace <tmp> --live`(真起一次老师)→ `serve --workspace <tmp>` → `curl /api/tutors?kid=1`;真跑老师:`cotutor send math-tutor "<消息>" --workspace <tmp>`(或页面 `/parent`)。手册三份:docs/家长手册.md(只用)、docs/开发者手册.md(改代码)、docs/iPad与iPhone.md(证书 / 信任开关 / 用 IP 不用 .local / 加到主屏幕,分设备各做一次)
- 在 Claude Code 会话里起 claude 子进程要 `env -u CLAUDECODE ...`(嵌套会拒);本机 claude 2.1.220 不认全局 settings 里的模型,运行时模板加 `--model sonnet` 才能跑(模型旋钮本来就在模板里);流式靠模板里的 `--include-partial-messages`(出厂模板有,老 workspace 的 cotutor.json 要自己加,不加就整块出),qwen 0.21.13 没有对应开关
- voxtell 没发 npm:从 TarsLab/voxtell 检出后 `pnpm add -g .` 进 PATH(pnpm ≥ 10 没有 `link --global`;首次先 `pnpm setup` 把 `~/Library/pnpm/bin` 加进 PATH 并重开终端),`tts.say` 出厂值就能用;不链就把 `tts.say[0..1]` 改成 `node <voxtell 检出目录>/bin/voxtell.js`;iPad 真机要 `cotutor cert`(mkcert)+ 把根证书装到 iPad

## 约定

- **证书是机器级的**(2026-09-09 拍板):签的是本机名与局域网 IP,与孩子无关,所以放 `~/.config/cotutor/certs/`(`USER_CERT_DIR`,与 `config.json` 同目录),不在 workspace 里;多孩子共用一张,IP 变了只重签一次
- **workspace 解析链**:`--workspace` > `COTUTOR_WORKSPACE` > cwd 或祖先有 cotutor.json > `~/.config/cotutor/config.json` > `~/cotutor/` 下唯一的孩子目录 > 报错附修复指南。没有 cwd 兜底,cotutor.json 是必需的政策文件;在但坏了响亮报错
- **init 幂等补缺**,已有文件一律不动;政策文件(cotutor.json)与家规(CLAUDE.md / QWEN.md)只在缺失时写模板。唯一例外:`.claude/agents/` 里指向包的旧链会被换成拷贝(链不是用户数据)
- **老师按 cotutor.json 里有谁走,没有注册表**:init / doctor 对表里每一位补目录与 `.qwen` 链、查文件;出厂五位多一层 hash 与 upgrade,自家加的(`cotutor add <name> --display …`:出模板文件、`patchConfig` 进表、建目录)永远是 untracked,upgrade 不碰,doctor 的 origin 行写「自家加的老师」
- **老师文件与 cotutor.json 的字段归属**:文件里只有运行字段 + 正文;display / avatar / voice / enabled / hidden / policy / runtime 全在 cotutor.json。policy 的 `board: auto | off` 决定要不要出卡,off 时上下文包带 `board: off`;`scenes.dailyMax`(缺省 2)= 每天最多起几个场景作业(一个 ≈ 一轮问答的 30 倍费用)。**已有 workspace 的 cotutor.json 不会自动多出新出厂件**(政策文件不动):差异由 `src/cli/migrate.ts` **现算**(拿这份自己的 slug 生成一份出厂模板去比),doctor 的 `config.migrate` 点名、`cotutor upgrade --config [--dry-run]` 与家长端设置页顶上的卡只**补缺**(新老师连文件与家一起补,命令模板里缺的旗标按出厂位置插进去),家长写过的值一个不动
- **给 scene-maker 的转交,课包 id 三处一致**:runner 起作业前 `settleBundleId`——refs 里没有合法 id 就取这节里 scene 卡的 bundle;scenes/<id>.ts|.md 或 bundles/<id>/ 已占用就改成 `<id>-2`,卡、refs、转交单一起改(2026-09-10 真跑:老师两次起了同一个 id,孩子卡上放了旧课包)
- **转交只写「## 转交」段,不派子代理**:claude 会把 `.claude/agents/` 里的老师文件当可派的子代理,老师自己 Task 派 scene-maker 会在自己那轮里烧自己的预算(2026-09-10 真跑见过);家规与数学老师文件都写了这条
- **孩子视图是机械规则**(2026-09-10 改板书):正文 = `kidSource`(这轮里**带围栏或带 H2 固定段的顶层文本段 + 最后一段**,顺序不变;`result.result` 只是最后一段,老师板书完再用工具、再补一句时板书不丢——真跑见过三次才定的规则;流式的 PartialReader 同一条),剥掉「## 待裁量」「## 转交」段之后,**正文就是板书**——普通段落是讲稿(一行一句,念出来),围栏是卡(标签 = kind);第一个 H2 起(「## 家长」)是给家长的尾巴,孩子看不到;kidText = 讲稿各句拼起来,每句按 replyMaxChars 截;答案(choice 的 `[x]`、fill 的 `= 答案`)下发前 `stripSecrets`。老师的写法在 agents/*.md 与家规里,真跑样本在 `tests/fixtures/board/`(解析器的回归样本,改解析器先过它们);这两种固定段只吃字段行,遇空行后不是字段行就结束
- **卡的状态是孩子的,存服务端**(2026-09-10):`conversations/<老师>/<日期>.<job>.cards/<n>.json` = `{at, turn, state}`,同目录 `<n>/<k>.mp3` 是后台生成的资产(点读段),turn 是存的时候索引末条的 job;发消息时 turn 等于当时末条的卡就是「上一轮之后改过的」,逐张 `describeCard` 进上下文包 `cards:` 段(带答案,老师口头说对错),消息记 `cards[]`,家长视图显示「孩子在板书上做的」;孩子端下发时并到 `section.cards[n].state`,重开页面选中态还在。状态文件不进索引、不需要重建
- **账本追加式**,同 id 后者为准,retracted 用追加行;坏行 doctor 点行号,不删账本
- **对话索引物化**:`conversations/<老师>/<日期>.json` 每条消息带 kidText / holdup / handoff / agent(运行时名)/ error,页面不解析日志;家长视图的转录行从 `.log` 现读(`foldRuns`,`sub` 行折成「子代理」)。换运行时(agent 不同)→ 新开会话且索引里的 session 换成新的;跨天 → 新文件自然不 resume
- **孩子端永远没有错误与评判**:`/api/kid/*` 只给问句 / 回复 / 配音 / pending(出错的运行不出现,家长的问句不露),每日上限 `dailyMessages` 只数孩子发的(「继续」不算,交答案算),到了 429 且首页头像灰;卡上不画对错,对错只在老师嘴里;页面上任何失败都只是「什么都不出现」或头像灰(后端不通 → `offline`),没有报错文案。识别在浏览器(webkitSpeechRecognition zh-CN 单句:按住 150ms 起、松手 stop 后 onend 直接发、上滑 60px abort;没有构造器 / 非 HTTPS / 没麦克风权限就只剩打字,服务端没有语音接口、不存声音),配音在服务端(没有退回 speechSynthesis)
- **cotutor.json 在页面上能改**:title / policyDefaults / tutors / runtimes.default / paths / server / tts,补丁深合并到原始 JSON(保留 `$schema` `_note`),整份过契约才落盘,null 删键;kid / version / 运行时模板走编辑器。老师文件正文走 `/api/tutors/<name>/file`(frontmatter name 必须不变),新老师 `POST /api/tutors` 与 `cotutor add` 同一条路,`DELETE` 只删自家的(文件改名保留)。服务每个请求看 mtime 热重载,改坏了留旧配置、`/api/health` 报 configError;**热的只有 cotutor.json**,页面与 runner 是代码,改了 `src/` 要重起 `serve`(老师文件每轮现读,不用)
- 两 CLI(2026-09-08 实测):claude 用 `--agent` 让老师文件当主会话(链上的定义能被找到,`memory: project` 落在老师目录);qwen 0.21.13 **没有** `--agent`(Unknown argument),但 `--append-system-prompt` 有且生效、`--approval-mode yolo` / `--yolo` / `--max-wall-time` / `--resume` 都在,stream-json 事件带 `parent_tool_use_id`——老师正文由应用读文件填进预设占位 `{agentBody}`
