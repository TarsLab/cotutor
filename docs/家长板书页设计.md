# 家长板书页:家长在 iPad 上看孩子的板书

> 2026-09-22 起;同日三期都落地:看(清单、板书、旁注、二维码、manifest)、试用(第五节)、家长真发与打星(第六节 3);只剩「试首页按钮的讲法」没做。起因:原来的 `/parent`(2026-09-22 起改叫工作台 `/dev`,拍板 13)是桌面页(没有一条 `@media`,`.stream` 定宽 940 加侧栏),面向配置与调试,要保留;家长拿起 iPad 想看「今天都学了什么、老师怎么讲的、孩子答了什么」,今天没有一页给这个用。第二个场景是**试用**:家长改了老师文件或 vault,想在 iPad 或 Mac 浏览器上看看老师现在会怎么讲,又不能进孩子的对话。拍板在文末。材料:`src/server/kid-page.ts`(板书渲染、只读回放、`__PREVIEW__`)、`src/lib/kid-view.ts` `kidConversation`、`src/server/app.ts` `kidDay` 与 `/parent/home-preview`、`src/server/parent-page.ts` 对话页、`src/server/replay.ts` `evalWorkspace`、`src/server/runner.ts` 记忆那处的 `replayOf` guard、《首页设计.md》§7.3、《产品规划.md》两个端。
> **词**:「板书页」= 孩子端的老师页,一位老师一个话题一页板书。「家长板书页」= 本文这一页,同一套渲染、家长的数据、只读。「旁注」= 家长板书页在节与节之间插的只读块(孩子说的、给家长的尾巴、记忆改动),**不是卡**。

结论先行:加一条家长侧的路由(当时是 `/parent/board`;2026-09-22 拍板 13 起就是 `/parent`,原 `/parent` 改 `/dev`),用孩子端页面本身渲染板书(和《首页设计.md》§7.3 首页预览一个做法:「渲染和孩子端一份代码,不会漂」),数据走家长接口(答案不剥、家长与系统发的话也在),节间插旁注,只读。家长在同一页上给老师开新话题,就是试效果与备课:孩子开口之前孩子看不到、不写记忆(第五节;2026-09-24 起,原来的 `try=1` 试用退掉)。孩子端一个字节不改;《产品规划.md》「孩子设备上没有通往家长端的入口」不动。

## 一、为什么不是孩子端「更多」里的一个开关

最初的想法是在孩子端「更多」菜单里放一个面向家长的开关,开了多显示几种「卡」。不这么做,三个理由:

1. **撞约定**。「孩子设备上没有通往家长端的入口」(《产品规划.md》两个端)。菜单里的开关就是入口,孩子一定会翻到。
2. **过滤失守**。记忆条目是老师对孩子的观察,家长尾巴是写给家长的评价,今天它们永远不进 `/api/kid/*`(《契约草案.md》§4,过滤在服务端做)。开关一开,要么孩子接口开始带家长数据,要么孩子页里长出第二条数据流去请家长接口。哪一种都在削「孩子端永远没有评判」这条约定。
3. **词会污染卡的协议**。仓里的「卡」是老师在围栏里写、`cards/<kind>/card.md` 八栏定义、点了进舞台、状态存服务端的东西。孩子的话、记忆改动不是老师写的、没有状态、不进舞台,做成卡种会让 `src/cards/` 注册表与 cotutor-board 技能多出一堆「老师不该写的卡」。

独立路由还有一个好处:后面迭代(家长发消息、打星、拍照)只动这一页,不碰孩子端的仲裁表。

## 二、页面

### 2.1 入口

- 路由 `/parent`,查询串 `?tutor=<名>&date=<日期>&thread=<话题>`;都不带就开在**今天的清单**(2.2)。
- `/qr` 页多印一个码「家长看」,编 `https://<主机名>.local:<端口>/parent`;工作台 `/dev` 顶部「扫码打开」旁有「家长端」。
- 自己的 manifest `/parent/manifest.webmanifest`(名字「<title> · 家长」,`start_url` 是这条路由),否则加到主屏幕拿到的是孩子端那份。
- 不设 PIN(见拍板 3)。一台 iPad 两个人用,主屏幕两个图标孩子都看得见——先看真用起来有没有问题,再决定要不要一个四位数。

### 2.2 今天的清单

家长在 iPad 上更常问的是「今天都学了什么」,是跨老师、跨话题的,板书页答不了,所以前面有一层:

- 顶上一个日期(缺省今天,能往前翻);下面每位有脸的老师一块:头像、显示名、今天几轮、花了多少。
- 块里一行一个话题:孩子第一句(截 20 字,首页按钮进来的标「首页 · 按钮字」)、几点、几节几张卡、停在哪(末句问句 / 在等孩子答 / 老师还在写)、家长打的星、记过账没有。
- 点一行进 2.3 的板书;家长、系统发起的话题也列(孩子端的「以前的」不列它们)。
- 行角上「删」(2026-09-22 加,孩子的与家长开的都有):点一下变「确定删?」,3 秒内再点才删,没有弹窗;`DELETE /api/conversations/<老师>/<日期>/threads/<id>` → `store.deleteThread`:索引里那个话题的消息、会话、星、记账标记去掉,那些轮的文件按 `<日期>.<job>.*` 整个删;老师还在写的 409。**不动**:vault 里的记忆与日记(家长的,去 Obsidian 改)、`captures/` 的照片;首页发布件里指向它的「接着」「再看看」按钮由既有的引用检查藏掉。
- 一位老师今天一句都没有就写「今天没聊」;后端不通整页灰,和孩子端一样。

数据和 cotutor-home 技能第二步读的是同一份(每位老师当天的索引),不另算。

### 2.3 板书

孩子端页面本身,`__MODE__ = "parent"`(见 4.1),差别只有四处:

| | 孩子端 | 家长板书页 |
|---|---|---|
| 数据 | `/api/kid/conversations/<老师>/<日期>` | `/api/conversations/<老师>/<日期>/board`(三、四) |
| 顶部 | 头像胶囊(回首页)、喇叭、更多 | 「‹ 清单」、老师与话题标签、喇叭;没有「更多」 |
| 底部 | 输入条(说话 / 打字 / 相机) | 没有;`S.readonly = true`,走「以前的」那条只读路径(选择 / 填空 / 画板不开) |
| 板书 | 节 | 节 + 旁注(三) |

- **只读**:不能发消息、不能改卡的状态、不能开新话题;卡照孩子端画,点一张卡进舞台只看(`readonly` 已经这么做)。
- **不自动念**:喇叭缺省关,家长想听哪句点哪句;不「从第一句播」。
- **老师还在写**:和孩子端一样流式露卡、轮询;孩子在另一台设备上答了,这边照服务端的状态重画(`loadDay` 已有这段)。
- **仲裁表不改**:`kid-board.ts` 的 `step` 一个字不动,家长页只是少发几种事件。
- 以上是看孩子的话题;家长自己开的话题(备课)见第五节。

## 三、旁注

家长比孩子多看到的东西。全是只读块,插在节的前后或卡的下面,样式在 `kid.css` 里加一组 `.note-*`,主题槽表不加新槽。

| 旁注 | 来源(索引字段) | 放哪 | 说明 |
|---|---|---|---|
| 孩子说的 | `from: kid` 的 `text` | 节前 | 首页按钮进来的(`via`)标「首页 · <label>」,不当孩子说的;`action: continue` 显示「继续」,`submit` 显示「交给老师」 |
| 家长 / 系统说的 | `from: parent / system` 的 `text` | 节前 | 标「家长」「系统」;孩子端不显示这些,家长页显示 |
| 照片 | `photos[]` | 孩子那句话下 | 经 `/api/kid/image?p=`(已有,只认 captures/ 里的) |
| 孩子在板书上做的 | `cards[]`(id + describe 出的那句) | 孩子那句话下 | 就是老师在这轮上下文包里看到的 |
| 卡的答案 | 不经 `stripSecrets` 的 `section` | 卡下一行灰字 | `choice` 的对错、`fill` 的答案;孩子答了的照孩子端画(状态在服务端),旁边灰字是答案,**不画对错标记** |
| 停下等孩子 | 末句 `ask` | 讲稿末句后 | 和家长端对话页一样缀「⏸ 停下等孩子」 |
| 给家长的 | `parentText` | 节尾 | 去掉「## 家长」标题 |
| 记住了 / 整理了记忆 | `remembered[]` | 节尾 | `tidy` 的那轮单独一节,只有这个旁注 |
| 记账 | `bookkeep` 那轮 | 话题尾 | 「已记进日记」一行;不展开记账段(日记在 Obsidian) |
| 提醒 | `warnings[]` | 节尾 | 灰字;「API 流断了 N 次」也在这里 |
| 出错 | `result: error` | 节位 | 孩子端什么都不出现,家长页出一行「这轮没成:<error>」 |

**不放**:思考摘要、工具行(`tools`)、用时(`timing`)、费用、后期结果(`post`)、会话 id、看原文。那是调试面,留在工作台 `/dev`。iPad 上的家长是在看孩子学了什么,不是在看进程。

## 四、数据与代码

### 4.1 接口

都在家长命名空间下,孩子接口(`/api/kid/*`)与 `kidConversation` 不动。

- `GET /api/conversations/overview?date=<日期>`:2.2 的清单。每位有脸的老师:`{ name, display, avatar, turns, costUsd, threads: [{ thread, at, title, via, sections, cards, stoppedAt: 'ask' | 'waiting' | 'writing' | null, rating, booked }] }`。
- `GET /api/conversations/<老师>/<日期>/board`:形状 = `KidDay`(`messages / pending / thread`,没有 `remaining`),每条 `KidMessage` 多几个可选字段:`from`、`via`、`action`、`cards`、`parentText`、`remembered`、`warnings`、`error`、`bookkeep`、`tidy`;`section` **不剥答案**;`question` 不管谁发的都给。
- `GET /parent` → `kidPage(title, { mode: 'parent' })`;`GET /parent/manifest.webmanifest`。

### 4.2 代码改动清单

| 文件 | 改什么 |
|---|---|
| `src/lib/kid-view.ts` | 加 `parentConversation(index, states, assets)`:和 `kidConversation` 同一个循环,不 `stripSecrets`、不丢 `bookkeep / tidy` 轮、`question` 不看 `from`、带上表里的字段。两个函数共用一个内部的 `withState`,免得漂 |
| `src/server/app.ts` | `parentDay`(照 `kidDay`,`partial` 也不剥)、`overview`、三条路由;`kidPage` 的第二个参数从 `preview` 改成 `{ preview?, parent? }` |
| `src/server/kid-page.ts` | `__PREVIEW__` → `__MODE__`(`{ preview?: 'draft' \| 'published', parent?: true }`);`PARENT` 模式:数据源(清单与板书接口)、chrome(没有输入条、「更多」、「回到今天」;喇叭缺省关)、`loadDay` 后 `syncNotes` 插旁注、`openTutor` 里 `S.readonly = true`。**分支只准出现在这四处**,别的地方看到 `PARENT` 就是写错了。旁注与清单的 CSS 在页面里(是 chrome,不是皮肤),主题不用跟 |
| `src/server/qr-page.ts` | `?to=parent` 第二个码,两张页脚互换 |
| `src/server/dev-page.ts`(拍板 13 前叫 parent-page.ts) | 顶部「家长端」链接 |
| `src/lib/icon.ts` | `webManifest(title, { startUrl, scope })`,家长板书页自己的一份 |
| `src/server/mock.ts` | mock 也起这条路由(不花钱看前端) |
| `scripts/probe-parent-board.mjs` | CDP 驱动:开清单 → 点话题 → 截图;iPad 尺寸 |

### 4.3 测试

- `tests/server.test.ts`:`/board` 有答案、有家长发的问句、有 `parentText / remembered`、出错的轮有 `error`;**同一份索引 `/api/kid/…` 的输出不变**(这条是本设计的护栏);`overview` 的 `stoppedAt` 三种;不认识的老师 404。
- `tests/kid-view.test.ts`:`parentConversation` 与 `kidConversation` 对同一索引的差集恰好是表里那几个字段。
- `tests/mock.test.ts`:`/parent` 与 manifest 能拿到,内联脚本能解析(模板字符串的坑照旧)。
- `tests/player.test.ts`:不改——仲裁表没动就不该动。

## 五、家长开的话题:试效果、备课

> 2026-09-24 重写(拍板 16):原来的「试用」(`try=1`,落 `evals/`,第二个 runner)退掉;09-22 那一版的全文在 git 历史里。

第二个场景没变:家长改了老师文件的正文、在 vault 里加了一节题型设计,想看老师现在会怎么讲;或者想先和老师把今天的课备好再给孩子。变的是做法:**不另开目录、不另设模式,家长在 `/parent` 上给某位老师开的新话题就是这个场景**。

- 清单每位老师一块右上角「新话题」(替掉「试一试」,「试过的」一栏去掉):进这位老师的板书页,空白态,输入条在。发的是家长真发那条接口(`from: parent`、`newThread: true`),落 `conversations/`,新会话,所以老师文件与 vault 的改动一定生效。
- 这种话题(第一条 `from: parent`)在孩子开口之前:孩子端看不到、记忆段不写 vault(原文进 `memoryDraft`,旁注「本来会记住的」)、不算上限、不记账;配音与后期照开;卡家长能做;每轮一个费用标签。
- 在里面可以接着聊、让老师重写;备课的结果是「这节课」——老师写的卡默认进课,不要的点灰(节头「整节不要」、点开卡顶栏「给孩子」开关),底部「这节课」条上「交给孩子」,首页多一个「接着」按钮,孩子看到的就是这几张卡,答了老师在同一个会话里接着讲(2026-09-25 定,待落地;现在代码里还是「从这里给孩子」+「对孩子藏起来」)。
- 规则、交给孩子、接口、测试都在《备课设计.md》,这里不重复。
- Mac 浏览器一样开这一页;要看手机版加 `?device=phone`。工作台 `/dev` 不做这个。CLI 不加命令:终端里要拿旧问题比差异仍是 `cotutor replay`(`evals/` 只剩它)。

## 六、分期

1. **第一期:只读**。清单、板书、旁注、二维码、manifest、mock、探针。验收:在 ray 上真跑一晚,孩子学完家长拿 iPad 从清单点进去,看得到孩子说了什么、答案、给家长的尾巴。
2. **第二期:试用**(第五节;2026-09-24 退掉,改成家长开的话题,见拍板 16)。`evals/` 的 runner、三条接口、页面的 `try=1`、清单里的「试一试」与「试过的」。验收:改一句老师文件、vault 加一节题型,在 iPad 上试一轮,听到、看到、做了卡,`conversations/` 与 vault 记忆一个字节没变。
3. **第三期:家长真发**(2026-09-22 落地)。看的模式下今天那页输入条在:打字 / 按住说话 / 拍作业照片(发照片屏复用《作业照片设计.md》),走 `POST /api/conversations/<老师>/messages|photos`(`from: parent`,多收 `device`);卡仍锁着(`S.readonly` 管卡与「继续」,`canSend()` 管输入条——家长不替孩子答题);清单每个话题行尾五颗星,`PUT …/threads/<id>/rating`。记账仍在 `/parent` 与 Claude Code。**没做**:试首页按钮的讲法(`&via=` 从 `home/draft.md` 取)——要给 `resolveVia` 加草稿来源,等有人真要再做。
4. **PIN、录屏**另议。

## 七、文档要跟着改的

- 《产品规划.md》「两个端」:家长端 = `/parent`(本页),原页成工作台 `/dev`。
- 《家长手册.md》§5 加一小节(看、试);《iPad与iPhone.md》§5 扫码处提第二个码;《开发者手册.md》replay 那段:`evals/` 只有回放。
- 落地后本文头注改成「已落地」;活过两轮再考虑进 CLAUDE.md。

## 拍板记录

**2026-09-22**:

1. **入口在家长侧**:`/parent`,不是孩子端「更多」里的开关。「孩子设备上没有通往家长端的入口」不动。
2. **渲染共用、数据分开**:孩子端页面本身加 `mode: parent`;数据走家长接口,`/api/kid/*` 与 `kidConversation` 不动、不剥答案的逻辑只在家长侧。
3. **不设 PIN**:先看真用起来有没有问题。
4. **旁注不是卡**:不进 `cards/`、不进 cotutor-board 技能。
5. **第一期只读**,不自动念;家长发消息与拍照放第二期。
6. 板书前面有一层今天的清单(跨老师、跨话题);清单与 cotutor-home 读同一份索引。

**2026-09-22(试用)**:

7. **试用落 `evals/`,和 replay 同一个目录、同一条 Runner 的路**;不另开目录、不另写一套隔离。
8. **试用不写记忆、不算上限、不记账、孩子看不到**;区别于 replay 的三处:配音开、后期开、话题内能接着聊、卡能做。
9. **试用在家长板书页上做(`try=1`),iPad 与 Mac 浏览器同一页**;工作台(当时叫 `/parent`)的 composer 仍是发真消息;CLI 不加命令。
10. **费用在试用的板书上显示**,只这一处;看的模式仍不显示。
11. 分期改为:只读 → 试用 → 家长真发。

**2026-09-22(删话题)**:

12. **清单上能删话题**,孩子的与试过的都能;删的是 workspace 里那个话题的一切(索引条目与 `<日期>.<job>.*` 文件),**vault 不动**——记忆与日记是家长的,删对话不等于删记录。两下确认,不弹窗;老师在写的挡住。

**2026-09-22(地址互换)**:

13. **`/parent` 归家长端**(本文这一页;家长不管有没有技术背景日常都进它,iPad 扫码进的就是它),**原 `/parent` 改 `/dev` 叫工作台**(对话原始视图、看原文、老师团、音色、设置、首页排版;`parent-page.ts` → `dev-page.ts`)。`/parent/board` 不留跳转(没发过版)。家长端上不放去工作台的链接,工作台从终端输出与手册找。
14. **记账搬到家长端清单上**(工作台照旧有):顶上一颗「记账」,两下确认(花钱),这天每位老师没记过的话题各起一轮;老师在写或在记就灰,行上「记账中」→「已记账」。三个词从此固定:孩子端 `/`、家长端 `/parent`、工作台 `/dev`。

**2026-09-22(备课)**:

15. **备课在试用页上做**,设计在《备课设计.md》:试用里能「回到这一节」(分叉,不动会话;2026-09-23 落地,在每节尾的旁注区)、能「排到首页」(把满意的开场原样复制给孩子,孩子答了老师新会话接着;二期)。

**2026-09-24(推翻试用)**:

16. **试用退掉,家长开的话题就是试效果与备课**(设计在《备课设计.md》,拍板 10–15):不落 `evals/`、不设 `try=1`、不起第二个 runner;清单上「试一试」改「新话题」,「试过的」去掉。推翻本文 7、8、9(10「费用只在这一处显示」改成「只在家长开的话题上显示」);15 按《备课设计.md》新拍板。
